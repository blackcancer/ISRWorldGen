# T11 — Tests principaux : Adaptateur Vintage Story et compatibilité du monde

**État initial de tous les cas : NOT_RUN.** Exécuter sur les fixtures déterministes de `registry/fixtures.json` et conserver seed/config/version/commit dans chaque preuve. Les seuils indiqués ici sont des obligations de test, non des mesures déjà obtenues.

## T11-01 — Cycle et propriété des passes
**Niveau : Intégration VS. Exigence : R11-01.**

**Préparer.** Trace complète d’une colonne, monde activé et témoin désactivé.

**Exécuter.** Vérifier ordre des hooks, comptage de placements, réexécution et fermeture.

**Accepter seulement si.** Une autorité par donnée, aucun double terrain, cœur sans références API ; handlers inconnus traités explicitement.

**Preuve.** Résultat structuré PASS/FAIL/BLOCKED, paramètres effectifs et artefacts ciblés ; ajouter TRX pour le test .NET, journal/processus pour le jeu et capture référencée lorsqu’une inspection visuelle est requise. Aucune capture seule ne valide une propriété numérique.

## T11-02 — Maps natives
**Niveau : Intégration VS. Exigence : R11-02.**

**Préparer.** Points témoins chauds/froids/humides/secs et côtes ; encodages audités.

**Exécuter.** Lire les maps, leurs paddings et les valeurs utilisées par les consommateurs du jeu.

**Accepter seulement si.** Climat/forêts/océans cohérents ; pas de BiomeMap magique ; RiversMap utilisé seulement si format vérifié.

**Preuve.** Résultat structuré PASS/FAIL/BLOCKED, paramètres effectifs et artefacts ciblés ; ajouter TRX pour le test .NET, journal/processus pour le jeu et capture référencée lorsqu’une inspection visuelle est requise. Aucune capture seule ne valide une propriété numérique.

## T11-03 — Heightmaps et couches
**Niveau : Intégration VS. Exigence : R11-03.**

**Préparer.** Terrain, arbre, caverne, lac et océan dans une fixture.

**Exécuter.** Comparer scans des blocs aux cartes aux étapes attendues puis recharger.

**Accepter seulement si.** Fond sous-marin distinct de niveau marin, RainHeightMap conforme à perméabilité, TopRockIdMap valide, fluides non écrits comme roches.

**Preuve.** Résultat structuré PASS/FAIL/BLOCKED, paramètres effectifs et artefacts ciblés ; ajouter TRX pour le test .NET, journal/processus pour le jeu et capture référencée lorsqu’une inspection visuelle est requise. Aucune capture seule ne valide une propriété numérique.

## T11-04 — Structures et réservations
**Niveau : Intégration VS + revue. Exigence : R11-04.**

**Préparer.** Accès de merveille, fleuve, minerai et emplacement story qui se chevauchent.

**Exécuter.** Exécuter toutes les passes natives retenues puis revérifier navigation/eau/structures.

**Accepter seulement si.** Aucun site accepté obturé ni structure obligatoire détruite ; conflits résolus par politique documentée.

**Preuve.** Résultat structuré PASS/FAIL/BLOCKED, paramètres effectifs et artefacts ciblés ; ajouter TRX pour le test .NET, journal/processus pour le jeu et capture référencée lorsqu’une inspection visuelle est requise. Aucune capture seule ne valide une propriété numérique.

## T11-05 — Spawn et progression
**Niveau : Intégration VS + jeu. Exigence : R11-05.**

**Préparer.** 16 seeds de jeu non choisies selon réussite ; profils de climat/monde disponibles.

**Exécuter.** Créer, vérifier dégagement, eau et ressources initiales, puis parcourir les étapes de survie de la recette.

**Accepter seulement si.** Spawn utilisable, pas d’apparition noyée/enterrée ; minerais et ressources critiques accessibles selon profil ; absence de dépendance fantastique.

**Preuve.** Résultat structuré PASS/FAIL/BLOCKED, paramètres effectifs et artefacts ciblés ; ajouter TRX pour le test .NET, journal/processus pour le jeu et capture référencée lorsqu’une inspection visuelle est requise. Aucune capture seule ne valide une propriété numérique.

## T11-06 — Serveur sans rendu
**Niveau : Multijoueur. Exigence : R11-06.**

**Préparer.** Serveur dédié cible et deux clients compatibles ; pas de GPU utilisé par Core.

**Exécuter.** Créer/explorer simultanément des frontières et comparer état serveur/clients ; requête debug par joueur non autorisé.

**Accepter seulement si.** Même terrain pour tous, aucune API client sur serveur, assets correctement exigés, export secret refusé.

**Preuve.** Résultat structuré PASS/FAIL/BLOCKED, paramètres effectifs et artefacts ciblés ; ajouter TRX pour le test .NET, journal/processus pour le jeu et capture référencée lorsqu’une inspection visuelle est requise. Aucune capture seule ne valide une propriété numérique.
