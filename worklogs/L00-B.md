# Passation — L00-B

**Statut proposé : REVIEW.** L'agent ne déclare pas la tâche `DONE`.

## Identité

- Tâche : `L00-B` — prouver le débogage C# par le MCP.
- Propriétaire : `agent:l00_b`.
- Branche/worktree : `agent/l00-b` / `[WORKTREE-L00-B]`.
- Base intégrateur : `381548c42215658d913cad564ecd1d2b024ce5fb` (`build: default Visual Studio debug to isolated server`).
- Commit de code propre effectivement construit et débogué : `965b1df672320f4458ad5c482c7a4c071a3eb90e`.
- Commit documentaire final : `HEAD` de `agent/l00-b`, communiqué dans la passation intégrateur.
- Contrats lus sans modification : `C06` et `C07`, version 1.0.
- Jeu/runtime : Vintage Story serveur 1.22.7, `net10.0` / .NET 10.0.11, x64.
- Sauvegarde de rejeu : `[WORKTREE-L00-B]\.local\VintagestoryData\Saves\l00b-review-965b1df.vcdbs`, seed `863010768`, monde standard `1024000 x 256 x 1024000`, marqueur `.isrworldgen-lab` présent.

## Résultat

`L00BDebugProbeModSystem` est maintenant entièrement entouré par `#if DEBUG`. Le type lui-même, son inscription worldgen, ses journaux de colonne, la demande de colonne et l'exception contrôlée sont absents du binaire Release. L'oracle inspecte directement les tables de métadonnées PE des assemblies compilées : type et méthodes attendus présents en Debug, type absent en Release.

En Debug, le probe est serveur seulement et non mutateur. Il inscrit un callback en lecture seule sur `ChunkColumnGeneration`, passe `Terrain`, monde `standard`. Deux activations restent `false` dans tout lancement normal et ne changent que par `debugger_set_variable` pendant T00-03 :

- `generateProbeColumn` planifie à `RunGame` une requête bornée `LoadChunkColumnPriority` dans le lab ;
- `throwRequested` déclenche une seule `InvalidOperationException` contrôlée grâce au garde `Interlocked`.

Une requête à `(31998,31998)` a entraîné 36 callbacks voisins nécessaires au moteur avant `OnLoaded`. Aucun bloc, carte, configuration ou donnée métier du mod n'est écrit ; les chunks sont limités à la sauvegarde jetable du rejeu.

Exigence couverte : `R00-03` sur la cible locale auditée.

## Audit API strictement nécessaire

- Assemblage local : `VintagestoryAPI.dll` 1.22.7.
- `ChunkColumnGenerationDelegate.Invoke` observé par réflexion : `void(IChunkColumnGenerateRequest request)`.
- Coordonnées inspectées : `IChunkColumnGenerateRequest.ChunkX` et `.ChunkZ`.
- Requête bornée : `IWorldManagerAPI.LoadChunkColumnPriority(int, int, ChunkLoadOptions)` et `ChunkLoadOptions.KeepLoaded/OnLoaded`, recoupés avec le XML local.
- Une session exploratoire antérieure a affiché via MCP une `IndexOutOfRangeException` lors d'une inscription sur `EnumWorldGenPass.Done`, mais ni transcript structuré ni log brut correspondant n'ont été conservés. Cette observation est donc qualifiée **transitoire, non reproductible dans les preuves livrées et non utilisée comme dépendance d'acceptation**. Le probe livré utilise `Terrain`, réellement compilé et exécuté avec succès.

## Capacités MCP réellement observées

| Capacité | Outil/capacité observée | Résultat |
|---|---|---|
| Solution | `solution_open`, `solution_info`, `solution_close` | PASS |
| Build | `clean_solution`, `build_solution`, `build_status` | PASS, `Done`, 0 projet en échec |
| Lancement | `debugger_launch` sans argument sur le projet de démarrage | PASS, serveur réel |
| Choix du profil | aucun outil MCP exposé | `NOT_AVAILABLE`; profil serveur rendu premier par l'intégrateur |
| Attache à un PID | aucun outil MCP exposé | `NOT_AVAILABLE`; non requis après F5 réussi |
| Breakpoints | ajout, liste et retrait MCP | PASS |
| Inspection | locals, evaluate et callstack MCP | PASS ; seule la frame managed courante est exposée |
| Reprise/exception | continue, step-over et status MCP | PASS |
| Arrêt | stop MCP puis statut `Design` | PASS |

`debugger_launch` avec le nom de projet avait échoué pendant l'exploration ; F5 sans argument est la procédure rejouée. Tout lancement éventuel du client `Vintagestory.exe` doit obligatoirement passer par le débogueur Visual Studio afin de conserver l'authentification entre sessions. Aucun client n'a été lancé pendant le rejeu de correction, et aucun client n'a jamais été lancé en CLI. Le serveur de laboratoire a été lancé uniquement par le profil Visual Studio isolé.

## T00-03 rejoué après REQUEST_CHANGES

La session finale porte exclusivement sur le commit propre `965b1df672320f4458ad5c482c7a4c071a3eb90e` :

- build Visual Studio Debug terminé avec 0 projet en échec ;
- `AssemblyInformationalVersion` évaluée dans le processus : `1.0.0+965b1df672320f4458ad5c482c7a4c071a3eb90e` ;
- copie immédiate de la DLL et du PDB construits avant F5 ; hashes inchangés après le processus ;
- `StartServerSide` atteint ligne 26 sur PID `12020`, module exact du worktree ;
- callback colonne atteint ligne 68, requête runtime `ChunkColumnLoadRequest`, coordonnées `(31998,31998)` inspectées ;
- pile inspectée : profondeur MCP exposée `1`, frame C# du probe avec fichier, ligne et module exacts ;
- `throwRequested` inspecté à `false`, muté à `true`, exception `InvalidOperationException` observée avec `ExceptionNotHandled` et `$exception` ;
- reprise effective, callbacks suivants et `L00B_PROBE_COLUMN_LOADED` observés ;
- tous les breakpoints retirés, débogueur `Design`, solution fermée, zéro processus jeu.

## Tests et preuves

| Test | Commande ou capacité réellement utilisée | Statut | Rapport / preuve |
|---|---|---|---|
| Probe analytique Debug | `Invoke-L00BStaticChecks.ps1 -Configuration Debug` | PASS | type/méthodes/champ présents dans les métadonnées PE |
| Probe analytique Release | `Invoke-L00BStaticChecks.ps1 -Configuration Release` | PASS | type L00-B absent des métadonnées PE |
| T00-03 StartServerSide | F5 MCP, breakpoint, locals/pile/evaluate | PASS | PID 12020, commit et module corrélés |
| T00-03 colonne | breakpoint callback, locals/pile/evaluate | PASS | `(31998,31998)`, profondeur exposée 1 |
| T00-03 exception | mutation locale, continue, step-over, status/locals | PASS | exception contrôlée unique et message exact |
| T00-03 reprise/arrêt | continue, traces ultérieures, stop/retrait/fermeture | PASS | `OnLoaded`, Design, 0 breakpoint/processus |
| Validation cryptographique | `Test-L00BDebugEvidence.ps1 -EvidencePath .local\L00B\evidence\T00-03.json` | PASS | commit existant, ProductVersion, chemins, tailles, hashes et contenus des logs vérifiés |
| Régression L00-A/T00-02 bornée | `Test-L00ALoad.ps1 -Configuration Debug -LogPath <server-main préservé>` | PASS | 1 DLL, 1 bootstrap, 1 ready, 0 erreur de chargement/référence |

### Artefacts exacts du rejeu

Tous les chemins ci-dessous sont relatifs à `.local\L00B\evidence\` et ne sont pas suivis par Git :

| Artefact | Octets | SHA-256 |
|---|---:|---|
| `artifacts\965b1df672320f4458ad5c482c7a4c071a3eb90e\ISRWorldGen.dll` | 8704 | `8D93932663672B017C569F1081558207FDE23EEB035B430321542D7F0B335E63` |
| `artifacts\965b1df672320f4458ad5c482c7a4c071a3eb90e\ISRWorldGen.pdb` | 13044 | `272966E1652DE2A37BC86B44CB094D285BBA21D641E6A42C53BF23C7D0FD76E1` |
| `logs\965b1df672320f4458ad5c482c7a4c071a3eb90e\server-main.log` | 879080 | `D8187760A16EE991C757F1E300C3A2773299BE207704C704FEC689FABAFFDA1F` |
| `logs\965b1df672320f4458ad5c482c7a4c071a3eb90e\server-worldgen.log` | 1462 | `603ADF7B0CBBE5F860054494496D584E24F9F8DC7A4BF01233B90DDA2E152F7C` |

La preuve structurée `.local\L00B\evidence\T00-03.json` est en schéma 2. Son validateur refuse les chemins absolus/échappant au dossier de preuve, vérifie chaque fichier, longueur et SHA-256, la signature PDB portable, l'existence Git du `TestedCommit`, la `ProductVersion` de la DLL et les marqueurs PID/hash/coordonnée des logs.

Le build Release rejoué après la session a produit la DLL SHA-256 `31739A75699CBA5B10F0240DD633E7E6799F0BAA9E439E12AC919C88330464A7` et le PDB `C8DD7EBC27419F40B1736A2729F6BC55F6C0573CE6200E964ACA8079B48ABBAA`; l'inspection PE confirme `ProbeTypePresent=false`.

## Contrats et impacts

Aucun contrat, projet, solution, registre, profil ou dépendance n'a été modifié par l'agent. Les changements restent dans `src/WorldGen.VintageStory/DebugProbe/`, `testsrc/WorldGen.Tests/L00B/` et ce rapport. Le réordonnancement du profil serveur appartient à la base intégrateur `381548c4`.

Pas de golden ni de schéma métier à mettre à jour. Le probe Debug est une instrumentation de test, pas la stratégie d'injection terrain de L00-C.

## Risques et limites

- Le MCP ne permet pas de sélectionner un profil ni d'attacher un PID. La procédure dépend du profil serveur placé en premier et vérifie immédiatement le processus après F5.
- `debugger_get_callstack` masque les frames moteur externes ; la frame managed courante reste corrélée au fichier, à la ligne et au module exacts.
- `debugger_stop` termine le processus débogué ; cela ne remplace pas T00-06 sur le cycle complet de sauvegarde.
- Une requête prioritaire peut générer les voisins nécessaires. Le type complet est absent en Release et les deux activations Debug sont false par défaut.
- L'observation exploratoire sur `Done` n'a pas de preuve brute durable et ne doit pas être promue en fait d'API sans campagne dédiée.

## Reprise exacte

- Statut proposé : `REVIEW`, en attente d'une nouvelle revue indépendante puis de l'intégration.
- Débogueur Visual Studio/MCP disponible mais en mode `Design` ; solution fermée ; 0 breakpoint ; 0 processus `Vintagestory`/`VintagestoryServer`.
- Données et preuves exclusivement sous `[WORKTREE-L00-B]\.local`; aucune sauvegarde personnelle lue ou modifiée.
- Branche à rendre propre après le commit documentaire final ; aucun push.
- À l'intégration : rejouer les deux configurations de l'oracle PE, le validateur T00-03 sur le dossier `.local` tant qu'il est conservé, puis la régression L00-A.
