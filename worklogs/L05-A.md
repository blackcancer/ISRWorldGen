# Passation — L05-A

**Statut proposé : REVIEW.** Ne pas déclarer DONE soi-même.

## Identité

- Propriétaire : agent/l05-a ; branche : `agent/l05-a` ; worktree : `ISRWorldGen-worktrees/l05-a`.
- Base vérifiée : `08295d8d48c35724d5b44a098351ac10760665cf`.
- Dépendance L03-B : `DONE` dans `registry/state.json` (lecture seule).
- Contrat consulté : C03 v1.0. Aucun contrat, registre, solution ou projet modifié.

## Résultat

`Hydrology/Depressions` introduit un Priority-Flood déterministe sur un graphe symétrique de cellules :

- le relief physique est immuable et distinct de l'altitude de routage ;
- les exutoires sont explicitement typés ; une composante sans exutoire devient un terminal `DryBasin` stable ;
- les receivers forment un DAG construit par la file prioritaire `(altitude, id)` ;
- les cellules rehaussées sont regroupées en objets `Depression` avec seuil et capacité analytique.

Correctif P1 après revue : le départage par ID ne s'applique qu'au receiver. Les cellules adjacentes d'un même fond plat sont regroupées dans un unique nœud de cuvette et sa capacité est multipliée par leur nombre.

R05-01 et R05-02 sont ciblées. R05-04/T05-04 restent hors périmètre du sous-lot reçu : aucune classification hydrique ou salinité n'est introduite.

## Tests et preuves

| Test | Commande | Statut | Rapport / preuve |
|---|---|---|---|
| T05-01 | `dotnet test testsrc/WorldGen.Tests/WorldGen.Tests.csproj -c Release -p:VintageStoryPath=D:\Jeux\Vintagestory --no-restore --filter FullyQualifiedName~L05A` | PASS | `.local/L05A/tests/p1-release/L05A-p1-release.trx`, 4/4 PASS, dont fond plat adjacent |
| T05-02 | même commande ; répétée en Debug | PASS | `.local/L05A/tests/p1-debug/L05A-p1-debug.trx`, 4/4 PASS |

Le Core compile en Release `--no-restore` avec 0 avertissement et 0 erreur; `dotnet format src/WorldGen.Core/WorldGen.Core.csproj --no-restore --verify-no-changes` et `git diff --check` sont PASS. La vérification de format de solution entière est `NOT_RUN` : cet exécutable `dotnet format` ne transmet pas `VintageStoryPath`, donc charge l'API Vintage Story absente et échoue avant le format; les builds/tests complets avec cette propriété ont, eux, chargé l'API et réussi.

## Contrats et impacts

Aucun changement de contrat. Aucun consommateur existant n'est touché : le dossier hydrologique était absent sur la base. Le futur consommateur doit fournir les voisins et les terminaux explicites ; l'API refuse la topologie non symétrique.

## Risques et blocages

La hiérarchie publiée couvre les composantes de remplissage Priority-Flood et leur capacité; la fusion Fill-Spill-Merge de réservoirs avec bilan eau est réservée au lot hydrologique suivant. Aucune validation moteur n'est pertinente pour ce noyau CPU analytique.

## Reprise exacte

Dernier état vérifié : validations ciblées et Core vertes; aucun processus ni verrou MCP. Committer seulement les trois chemins autorisés, remettre le SHA à l'intégrateur sans fusion ni push.

## Enrichissement de robustesse — 2026-09-08

- Branche/worktree : `agent/l05-a-enrichment`, `E:\Développement\Vintage Story\ISRWorldGen-worktrees\l05-a-enrichment`.
- Base vérifiée : `6a9ba3e7c2f04a4987ecb1bc5c348015a8c18231`.
- Lectures supplémentaires, motivées par l'orchestration : L05-B/C (débits et ports), L07-A/B (corridors et water bodies) et L08-A (réseaux de cavernes). Aucun contrat, registre, projet, version ou dépendance n'a été modifié.

`DrainageTopology` reste un snapshot Core immutable et publie maintenant, en plus du DAG et de la hiérarchie, une connectivité exhaustive par cellule (`CellId`, `TerminalCellId`, type terminal) et un état hydrique déterministe. L'appel optionnel à niveau marin explicite distingue `OceanConnected`, `ClosedLake`, `DryBasin`, `SubmarineDryBasin` et `OutOfDomain`, sans remplir le relief physique. Ainsi une lagune/région ouverte est uniquement signalée connectée à un terminal `Ocean`; la géométrie qui permettra à L07-B de la nommer/mailler comme lagune reste délibérément différée.

Correctif démontré par l'oracle de connectivité : les états résolus partageaient auparavant l'enregistrement du terminal, donc l'index de sortie ne contenait pas le `CellId` de chaque cellule. La publication reconstruit désormais un état immutable par cellule, trié par ID. Les capacités qui débordent la plage `double` finie et les niveaux marins non finis sont rejetés explicitement, plutôt que de publier `Infinity`.

Les oracles enrichis vérifient : permutations d'entrées et de voisinages, selle/sorties équivalentes, DAG et terminaux explicites, graphes invalides, cuvettes et plateaux emboîtés, distinction mer/connectivité contre bassin sous-marin et lac fermé, et extrêmes numériques. Les besoins laissés aux lots suivants sont les bilans eau/volume (L05-B), les BoundaryPorts et budgets de raffinement (L05-C), la géométrie eau/côte/delta (L07-A/B), et les interfaces hydrogéologiques de cavernes (L08-A).

| Validation | Commande | Statut | Résultat |
|---|---|---|---|
| L05-A enrichi Debug | `dotnet test testsrc\\WorldGen.Tests\\WorldGen.Tests.csproj --no-restore --filter FullyQualifiedName~L05A -p:VintageStoryPath=D:\\Jeux\\Vintagestory` | PASS | 7/7 |
| L05-A enrichi Release | même commande avec `-c Release` | PASS | 7/7 |
| Régression L03-A Release | filtre `FullyQualifiedName~L03A`, `-c Release` | PASS | 13/13 |

La restauration a été requise une fois avant les tests car ce worktree ne contenait pas `project.assets.json`; elle a réussi. Les oracles sont Core, donc aucun processus Vintage Story n'est revendiqué. Le résultat proposé reste `REVIEW`; l'intégrateur doit relire, fusionner et relancer selon sa campagne.
