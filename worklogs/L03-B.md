# Passation — L03-B

> **Historique superseded.** Les références ci-dessous à `f67f594`, aux vues V/Q,
> aux résultats 8/8 et à la régression 213/213 ne sont pas des preuves du candidat
> courant : `f67f594` est REJECTED, Q a échoué la revue diagnostique (4/6), R est
> SUPERSEDED et S est NOT_RUN. Seuls les commits postérieurs soumis à une nouvelle
> re-revue pourront être présentés comme candidats.

## Correctif de protocole S — reçu aveugle durable avant reveal — 2026-09-08

Le commit `ca4fd5e` et toute coordination antérieure restent **REVIEW_REQUIRED** :
le bundle ne fournissait qu'un gabarit à réponses nulles et une consigne, sans reçu
durable antérieur au dévoilement. Aucune revue antérieure ne qualifie donc ce
correctif, et aucune campagne n'a été relancée ici. Le premier palier `c7d3f62`
est lui aussi superseded : il acceptait un emplacement de reçu choisi par appel et
une requête qui pouvait ne contenir qu'un sous-ensemble des six codes. Le palier
`ff52403` est également superseded : un alias NTFS ou une copie identique du blind
sous une autre hiérarchie pouvait encore publier un second reçu pour le même run.

Le paquet PASS publie désormais une requête de revue schema 2, sans champs de
réponse préremplis, et une clé scellée schema 3 liée au run, commit, tree, blob
fixtures, configuration, assemblies, manifeste blind et engagements. La clé porte
le statut `SEALED_AWAITING_VERIFIED_RECEIPT`; sa lecture directe ne constitue pas
un reveal valide. Le seul chemin de contrôle qualifié est le suivant :

1. un compte ou agent reviewer reçoit un accès lecture limité au dossier physique
   exact `<repo>/.local/L03B/evidence-s-terminal-<commit>/blind/` et un droit de
   création seulement sur son sibling review canonique, jamais une copie du blind
   ni un accès au sibling `sealed/` ;
2. la requête `T03-06-S-review-request.json` schema 2 est obligatoire, hashée par
   le manifeste et fermée sur exactement `S01..S06` et les six familles attendues ;
3. le reviewer renseigne exactement six identifications, confiances JSON entières
   `0..100` et observations JSON chaînes non vides; aucun cast de `70.6` ou d'une
   observation numérique n'est admis ;
4. `New-L03BBlindReviewReceipt.ps1` ne possède aucun chemin `sealed/`, revérifie le
   manifeste, la requête et tous les artefacts blind, puis exige le nom de terminal
   officiel lié au commit et le `runId` exact
   `evidence-s-staging-<commit>-<32hex>`. Racine, terminal, blind et artefacts
   doivent être des objets physiques directs, sans symlink, junction ni autre
   reparse point. La cible unique est toujours
   `<repo>/.local/L03B/evidence-s-review-<runId>`; `-ReviewParent` ne peut que
   confirmer cette racine. Le reçu est publié par
   `CreateNew + Flush(true) + Directory.Move` ;
5. le reviewer transmet seulement son `receiptFileSha256` imprimé ;
6. `Open-L03BBlindReview.ps1` n'accepte aucun chemin de reçu et applique la même
   identité physique au `EvidenceDirectory`, au blind et au reçu; un alias du
   terminal ne crée donc ni second lock ni second reveal. Il maintient l'unique
   fichier reçu canonique ouvert sans partage d'écriture,
   vérifie hash attendu, auto-engagement `receiptId`, heure UTC, six réponses et
   binding complet avant l'unique frontière de parsing de la clé ;
7. le reveal est publié atomiquement dans le sibling canonique du terminal. Un
   second reveal, un autre reçu, un replay inter-run, une modification avant ou
   après reveal, un fichier partiel ou supplémentaire sont refusés.

Le reçu ne contient ni nonce, ni opening, ni clé, ni mapping dévoilé. Il engage
le hash et la signature du manifeste blind et du bundle d'engagements; ces derniers
engagent à leur tour les artefacts, le commit, le tree et l'identité de run. Le
reveal conserve sans réécriture les réponses pré-dévoilement et ajoute seulement
la famille révélée et la correction de l'identification.

Les tests de contrat C# couvrent le schéma fermé, l'ensemble exact des six codes,
l'auto-hash, la requête obligatoire et les mismatches. Les tests d'intégration
exécutent les deux vrais scripts PowerShell sur des dossiers temporaires et
couvrent : absence de reçu, copie identique refusée, vrais junctions NTFS refusés
comme alias externe et comme remplacement du blind ou du terminal officiels, deux
soumissions avec des parents différents, reçu rogue ignoré par le contrôleur,
requête incomplète, confiance `70.6`, observation numérique, headers JSON
fractionnels manifest/commitments/receipt, hash attendu différent, reçu modifié,
clé sentinelle invalide non atteinte, succès, second reveal, mutation
post-reveal et replay inter-run. Le test
statique confirme que l'outil reviewer et son module n'ont aucune capacité
`sealed/`, `review-key` ou nonce, que les deux CLI n'acceptent aucun
`-ReviewDirectory` et qu'il n'existe qu'une frontière explicite de lecture de clé
côté contrôleur.

Gates de ce troisième palier : test ciblé `EvidenceProtocolTests` Debug
`14/14 PASS`; tests L03-B Debug et Release hors `EvidenceArtifactTests`
`59/59 PASS` dans chaque configuration, y compris les scripts reviewer/controller
et les junctions NTFS réels; builds solution Debug et Release PASS avec
`-p:VintageStoryPath=D:\Jeux\Vintagestory`, zéro avertissement et zéro erreur;
format solution `--no-restore --verify-no-changes` PASS avec `VINTAGE_STORY`
pointant vers la même installation; parse PowerShell des trois scripts/modules
touchés et `git diff --check` PASS. Evidence/S, preflight, jeu, Visual Studio/MCP
et push restent **NOT_RUN**.

## Correctif post-dévoilement S — cœurs purs et mesures non masquées — 2026-09-08

Le verdict post-dévoilement demeure **REVIEW_REQUIRED** : les cartes S historiques
centrées sur un site pouvaient inclure une bande de transition Voronoï, et le
rapport corpus ne conservait pas de mesure morphologique par famille et par seed.
Ce correctif prépare un nouveau candidat mais ne relance, ne lit et ne transforme
aucun artefact S existant.

La sélection aveugle essaie, en ordre stable, des emprises de `1,7` à `0,35` fois
la longueur macro de la famille. Une carte n'est admise que si chacun de ses
`193×193` pixels publiés a le même propriétaire et la même famille, exactement un
contributeur, `IsTransition=false`, poids primaire `1`, poids et contribution
étrangers `0`. Sinon l'emprise est réduite ou la fixture suivante est essayée;
l'absence de vue pure minimale fait échouer la campagne avant publication. Le
paquet aveugle conserve largeur/hauteur, span, pas nominal, offsets physiques exacts
des deux axes et un masque PGM explicite (`0=pur`, `255=transition`), lui aussi
couvert par les hashes du manifeste. Le renderer, les profils et les mesures
repassent tous par le même garde de pureté.

Pour chacun des 192 seeds de calibration (`balanced`) et des 64 seeds holdout
(`vast-expeditions`), le rapport terminal contient désormais exactement une entrée
par famille : `MEASURED`, `FAMILY_ABSENT` ou `NO_PURE_VIEW`. Une entrée
mesurée conserve la cellule, le centre, la grille pure `65×65`, son emprise/pas,
les mesures de pente, courbure, anisotropie, extrema, plateau, rim et
caldeira/cône, puis chaque trait attendu avec valeur, comparaison, seuil et booléen.
Les agrégats sont `failures/attempts`, taux et liste exacte des seeds en échec,
séparés par cohorte/famille/trait. Aucune médiane ni percentile inter-seeds ne peut
transformer un échec individuel en succès. Les seuils historiques de régression
morphologique ont été repris tels quels comme traits descriptifs; aucun seuil de
qualité ou de recette n'a été abaissé, et les échecs éventuels restent visibles
sous `OBSERVED_FAILURES_RETAINED` au lieu d'être masqués.

Le paquet aveugle fournit aussi un formulaire de revue immuable. Le relecteur doit
en copier le gabarit hors bundle, renseigner identification, confiance `0..100` et
observations pour chaque code, puis dater et hacher cette réponse avant de demander
l'ouverture de la clé scellée. Après dévoilement, seule la correction de chaque
identification est ajoutée; les choix et confiances pré-dévoilement ne sont pas
réécrits.

Régression nouvelle : `EvidenceViewContractTests` construit les six familles sur
le profil réel `vast-expeditions`, valide les cartes aveugles `193²`, leurs masques
et les mesures `65²`. Sept cas négatifs mutent ensuite indépendamment un seul champ
du même pixel : propriétaire, famille, `IsTransition`, nombre de contributeurs,
poids primaire, poids étranger ou contribution étrangère. Extraction d'altitudes,
masque et mesure refusent chacune des sept contaminations; la suppression isolée
d'une garde ne peut donc pas rester couverte par une autre mutation cumulée.
`EvidenceProtocolTests` vérifie séparément que le formulaire exige identification et
confiance avant la clé et refuse des codes aveugles invalides ou dupliqués.

Gates exécutés sans campagne `EvidenceArtifactTests` : build solution Debug et
Release avec `-p:VintageStoryPath=D:\Jeux\Vintagestory`, chacun `0 warning / 0 error`;
tests L03-B Debug et Release hors campagne `47/47 PASS`; format solution
`--no-restore --verify-no-changes` PASS avec `VINTAGE_STORY` pointant vers le même
chemin (le CLI `dotnet format` n'accepte pas `-p:`); `git diff --check` PASS. Une
première invocation de format sans cette variable a échoué au chargement des API
Vintage Story, sans modifier de fichier; les builds/tests explicites ci-dessus ont
ensuite confirmé la référence correcte.

Limites exactes : la campagne S et son corpus complet restent **NOT_RUN**; aucun
taux de trait, bitmap, formulaire rempli ni verdict humain nouveau n'existe encore.
Le jeu, Visual Studio/MCP, une sauvegarde, un push et le runner Evidence sont
**NOT_RUN**. La provenance HEAD/tree/blob fixtures, le détachement, les hashes
d'assemblies et le scellement atomique du runner demeurent inchangés. Le statut
global ne peut donc pas être promu au-delà de **REVIEW_REQUIRED** par ce commit seul.

## Correctif morphologique en cours — 2026-09-07

Le candidat `f67f594` a été rejeté en revue aveugle : les six vues dérivaient d'une
onde trigonométrique commune à axes fixes. Le correctif local remplace ce socle par
des primitives géométriques distinctes, toutes évaluées en coordonnées globales et
avec paramètres sans état : réseau de crêtes orientées, dômes érodés, superellipse
à sommet plat et escarpement, bowl elliptique à rim/strates, bruit lisse non
périodique de faible amplitude, et cônes/caldeira. Les amplitudes restent consommées
par `LandscapeModel.SampleCell`; aucun contrat ni profil vertical n'est modifié.

Cette révision est volontairement intermédiaire : la campagne Q01..Q06, la clé
séparée et la preuve finale ne seront créées qu'après intégration du correctif de
provenance L03-A, car la validation croisée Atlas/PlateAtlas et le parent Atlas du
checksum sont obligatoires avant un nouveau verdict. T03-05 reste inchangé.

## Reprise après scellement L03-A — 2026-09-07

Les anciennes preuves `V01..V06`, métriques et toute mention de succès automatisé
T03-06 ci-dessous sont **REJECTED / superseded** : elles visualisaient la signature
brute au lieu de la composition réellement publiée et ont échoué la revue aveugle.
Le correctif local inclut le scellement Atlas/PlateAtlas avant toute publication,
un checksum binaire canonique lié à l'identité complète et aux parents, un probe de
mélange `(identityA, atlasB, platesA)` refusé avant échantillonnage, un probe de
culture hostile et un ancrage des morphologies au site propriétaire, y compris un
probe de translation éloignée `vast-expeditions`.

La nouvelle campagne a des codes Q01..Q06 et une clé séparée sous
`.local/L03B/evidence-q/T03-06-Q-review-key.json`; ne pas l'ouvrir avant revue.
Les cartes utilisent `LandscapeModel.Sample`, donc le mélange, l'amplitude et la
composition réels. Son corpus 192 balanced + 64 vast reste **BLOCKED** sans
exclusion : l'instrumentation a localisé le premier appel non terminant à
`AtlasGeometryBuilder.Build` du seed balanced `-2121536160`, étape
`atlas-geometry`. Ce diagnostic a ensuite été infirmé par des exécutions isolées :
les seeds concernés terminent normalement. La campagne monolithique n'émettait pas
de heartbeat VSTest et le timeout Blame à 90 s, puis un arrêt manuel prématuré,
ont interrompu une campagne longue. Il ne s'agit donc pas d'un défaut L02/L03-A.
Le rapport Q historique demeure `RUNNING` et ne vaut ni PASS ni preuve finale ; la
reprise utilise un nouveau répertoire terminal et un suivi externe de progression.

La revue P2 a ensuite relevé un préflight de provenance tardif, une limite ponctuelle
au site et des paramètres de relief publiés mais non consommés. Q est donc
**superseded**, même si sa campagne a terminé : le correctif suivant place les quotas
avant la canonicalisation, rend le mélange continu et consomme toutes les échelles et
poids. Toute nouvelle revue utilisera seulement les codes R01..R06 et une nouvelle
clé séparée.

La tentative locale `dfe4ded` a été rejetée avant remise : la matrice signalait une
composante plateau et une variance volcanique insuffisantes. Les corrections
ultérieures rendent les profils hostiles refusés, consomment les six paramètres de
forme pour chaque famille et vérifient le quota avant toute provenance allouante,
ainsi que la continuité site intérieur `x-1/x/x+1`. Les campagnes Q et R restent
superseded ; aucune campagne S n'a été lancée.

**Statut proposé : REVIEW.** Le sous-agent ne déclare pas `DONE`. Les assertions de R03-05 sont vertes ;
les mesures de R03-06 sont vertes selon la politique provisoire pré-déclarée, mais le verdict qualitatif
reste `REVIEW_REQUIRED` jusqu'à la revue indépendante des vues aveugles.

## Correctif statique en cours — bornes et supports compacts

Les candidats locaux `19dd8e0` et `42142c9` sont **REJECTED** : le premier utilisait
un rabattement uniforme de signature et un top-k; le second évaluait bien un mélange
continu, mais chaque site de l'atlas à chaque point. Ces traces ne sont pas des
preuves de recette.

Le correctif en cours retire tout clamp/rabattement terminal : les six primitives
sont bornées analytiquement par leurs amplitudes, coefficients et poids convexes.
Les facteurs Plateau et Basin ont été réduits dans leurs formules respectives pour
que même un profil public avec un seul poids actif reste strictement dans `(-1,1)`.
Le checksum de famille passe à `V3-ANALYTIC-BOUNDS`; le checksum modèle passe à
`V5-COMPACT-VORONOI-SUPPORT`, ce qui sépare ce candidat de l'algorithme rejeté.

`FalloffExponent` est remplacé par `SupportOverlapFactor` `[1.05,1.5]`. Chaque rayon
est dérivé du sommet le plus éloigné de la vraie cellule de Voronoi (avec repli fini
sur les coins du monde pour topologies point/linéaire), puis recouvert par ce facteur.
Le noyau compact `(1-d²)²` est positif à l'intérieur et joint exactement zéro avec
pente nulle à la frontière : aucune sélection top-k à poids non nul et aucun
échantillonnage de morphologie hors support. Les régressions ciblent les anciens
échanges de rang `(896,1020)->(896,1021)` et `(2104,1024)->(2105,1024)`, les bords,
coins et topologies point/linéaire/planaire; elles ne constituent pas un seuil T03-06.

Le probe de contributeurs à facteur `1.25` mesure respectivement `1..11` (moyenne
`5.72`) sur 16 sites laboratoire, `1..10` (`5.34`) sur 64 balanced et `1..11`
(`4.81`) sur 128 vast-expeditions. Le scan des distances reste linéaire sans index
réutilisable dans le périmètre L03-B, mais `SampleCell` — la partie morphologique
coûteuse — est bornée localement et n'est plus évaluée pour chaque site.

La grille de couverture/contributeurs est désormais de `33×33` positions par profil,
auxquelles s'ajoutent explicitement les quatre coins. Elle mesure seulement la portée
locale du noyau sur les fixtures 16/64/128, et non une garantie de performance moteur.
Les deux anciennes coordonnées de saut utilisent une marge de régression `0.0011`
autour du maximum indépendant observé d'environ `8.1e-4`; cette marge n'est pas une
politique de recette T03-06.

La campagne future est préparée, pas exécutée : elle produira uniquement les artefacts
S01..S06 dans un répertoire strict `evidence-s-terminal-<commit>` absent au départ.
`blind/` ne contient que cartes, profils, métriques et un manifeste à codes neutres;
`sealed/` contient nonce, clé, rapport et heartbeat. Le manifeste atomique est d'abord
`RUNNING`, puis terminal avec hashes des artefacts aveugles et signature de bundle; il
ne contient ni famille, ni seed, ni site, ni checksum de profil. Le lancement futur
passe par `testsrc/WorldGen.Tests/L03B/Run-L03BEvidenceS.ps1`, depuis un worktree
détaché neuf : préflight HEAD/tree/status (y compris non suivis), build Release versionné
`+commit`, hashes des assemblies réellement chargées, test `--no-build`, puis
revalidation finale. Toute validation échoue avant la création du répertoire, dont le
verrou `CreateNew` est acquis avant les dossiers. S demeure **NOT_RUN**.

La mesure Debug après reformulation, sur 257 coordonnées déterministes par famille,
est la suivante (plage signature; variance) : RuggedRanges `[-0.65539;0.14838]`,
`0.03402`; OldMassifs `[-0.64137;0.12450]`, `0.03794`; Plateaus
`[0.15768;0.43155]`, `0.000531`; SedimentaryBasins `[-0.66081;0.00749]`,
`0.03372`; Plains `[-0.05056;0.22936]`, `0.002660`; VolcanicDomains
`[-0.49719;0.04970]`, `0.005842`. Ce sont des mesures de régression, pas une
campagne qualitative ni une levée de `REVIEW_REQUIRED`. Les profils catalogue et
les six familles à ratio de longueurs extrême admis (`1_000_000/2/1`), avec chaque
poids isolé et coordonnées/ancres opposées aux bornes, sont également couverts.

## Identité

- Tâche : L03-B — Composer reliefs et enveloppe verticale.
- Propriétaire : `agent:l03_b`.
- Branche / worktree courant : `agent/l03-b-fix` / `E:\Développement\Vintage Story\ISRWorldGen-worktrees\l03-b-fix`.
- Base publiée courante : `2d29c534b469ba394e6a8217b8c3357062f600bb` (`origin/main`).
- Commit historique REJECTED (non fonctionnel pour la recette courante) : `f67f59404bd2be4fd0c65a94152d7b948efd3f3f`.
- Commit de passation : commit contenant ce fichier ; son hash exact est fourni dans le message de remise.
- Contrats lus et inchangés : `C01 v1.0`, `S03 v1.0`, `T03`.
- Prérequis vérifié avant édition : `registry/state.json` indiquait `L03-A = DONE`.
- Runtime qualifié : .NET SDK `10.0.400`, runtime `10.0.11`, cible `net10.0`, Windows.
- Jeu / Visual Studio MCP / sauvegarde : `NOT_RUN`, non requis pour T03-05/T03-06 ; aucun verrou acquis.

## Résultat

R03-05 est implémentée par un préflight typé qui reçoit le vrai `FrozenScaleProfile` L02-C. Le plan
réserve explicitement le plancher rocheux, une hauteur intérieure de caverne, la couverture de caverne,
la profondeur océanique, le relief au-dessus de la mer et la marge supérieure. L'invariant inférieur est
`floor + cavernInterior + cavernCover + oceanDepth <= SeaLevel`; l'invariant supérieur exige strictement
`SeaLevel + relief < MaximumExclusiveUsableLevel`. Un preset incompatible retourne
`GenerationFailureCode.BudgetExceeded` avant de publier le transformateur. Les overflows arithmétiques
sont également refusés avec une erreur typée.

`AltitudeBlockTransform` est l'unique transformation publiée : `-1..0` correspond à l'océan le plus
profond jusqu'au niveau marin, `0` correspond exactement à `SeaLevel`, et `0..1` au niveau marin jusqu'au
relief maximal. Sa sortie continue en blocs est strictement croissante. Sa représentation de frontière
réutilise `HeightQuantizer` à 1/256 bloc et est non décroissante. NaN, infini et valeurs hors `[-1,1]`
sont refusés ; aucun clamp final ne masque un dépassement.

R03-06 fournit les six familles V1 : chaînes accidentées, vieux massifs, plateaux, bassins sédimentaires,
plaines et domaines volcaniques. Chaque profil possède des longueurs d'onde macro/méso/détail, des poids,
une amplitude et un checksum de paramètres distincts. Les signatures emploient des coordonnées monde
globales et le PRNG sans état existant ; elles ne dépendent ni d'une tuile, ni d'un `Random` partagé, ni de
`GetHashCode`, de l'horloge ou de l'ordre des threads.

`LandscapeModelBuilder` consomme explicitement `GenerationIdentity`, `FrozenScaleProfile`, `AtlasMesh` et
`PlateAtlasSnapshot`. Il vérifie hash de configuration, bornes, métadonnées de profil, budget et identité
exacte des cellules avant publication. Il conserve le checksum parent des plaques, copie et trie les
entrées, puis compose croûte, âge, uplift/subsidence et signatures multi-échelles sans modifier les objets
L03-A. Les intérieurs de plaques sans champ de frontière sont classés en plaines calmes. La bathymétrie est
publiée séparément de l'altitude. **Historique superseded :** la phrase qui suivait
décrivait un mélange fixe de quatre sites et ne s'applique à aucun candidat courant.

Fichiers modifiés :

- `src/WorldGen.Core/Geology/Landscapes/LandscapeFamilies.cs`
- `src/WorldGen.Core/Geology/Landscapes/ReliefVerticalBudget.cs`
- `src/WorldGen.Core/Geology/Landscapes/LandscapeModel.cs`
- `testsrc/WorldGen.Tests/L03B/L03BTestSupport.cs`
- `testsrc/WorldGen.Tests/L03B/VerticalReliefBudgetTests.cs`
- `testsrc/WorldGen.Tests/L03B/LandscapeCompositionTests.cs`
- `testsrc/WorldGen.Tests/L03B/EvidenceArtifactTests.cs`
- `worklogs/L03-B.md`

Aucun `.csproj`, solution, contrat, registre, plan, version, NuGet, `SHA256SUMS.txt` ou autre lot n'a été
modifié. Aucun stub et aucune dépendance jeu/GPU n'ont été ajoutés.

## Politique de campagne figée avant mesures

T03-06 ne donne pas de seuil numérique normatif. Avant la première campagne, les alarmes de projet
suivantes ont été fixées dans le test, sans calibrage ultérieur : variance de chaque fixture supérieure à
`0,0001`, différence absolue moyenne de chaque paire supérieure à `0,035`, et au moins cinq familles
présentes dans le corpus non filtré. Elles détectent absence de signal, signatures quasi identiques et
effondrement du catalogue ; elles ne prouvent pas qu'un humain reconnaît le paysage attendu.

La première campagne complète, exécutée avant tout ajustement, a passé 8/8 tests Debug. Aucun seuil n'a
échoué et aucun paramètre ou seuil n'a été recalibré. Les 192 seeds `calibration_seeds` utilisent le profil
réel `balanced`; les 64 seeds `holdout_seeds` utilisent le profil réel `vast-expeditions`. Les 256 cas sont
conservés, sans exclusion des seeds ni filtrage des résultats.

Les six fixtures ciblées sont publiées comme `V01` à `V06`. Le rapport et les fichiers de profil ne donnent
aucune étiquette de verdict ou de famille. La clé famille↔code est un fichier distinct que le contrôleur ne
doit ouvrir qu'après inspection des cartes et profils. Mesures aveugles observées :

| Vue | Moyenne | Variance | pente absolue moyenne | rugosité pas 4 | rugosité pas 12 |
|---|---:|---:|---:|---:|---:|
| V01 | 0,000151 | 0,203374 | 0,176612 | 0,373251 | 0,447965 |
| V02 | -0,194402 | 0,116593 | 0,348338 | 0,296724 | 0,280271 |
| V03 | 0,000446 | 0,112699 | 0,051482 | 0,148550 | 0,303948 |
| V04 | -0,458751 | 0,095774 | 0,160531 | 0,385240 | 0,386311 |
| V05 | -0,002084 | 0,096613 | 0,143595 | 0,321631 | 0,383934 |
| V06 | -0,497885 | 0,025232 | 0,047826 | 0,095679 | 0,177606 |

Dans le corpus non filtré, les familles apparaissent respectivement dans 248 à 256 cas sur 256 : vieux
massifs 253, plaines 256, plateaux 248, chaînes accidentées 255, bassins sédimentaires 256, domaines
volcaniques 255. Les altitudes modèle échantillonnées restent dans `[-0,712594 ; 0,503829]`; la bathymétrie
maximale observée va de `20,092459` à `45,606012` blocs selon le cas.

Budgets verticaux testés :

| Profil | monde | mer | plancher | couverture | marge haute | fond océan | plafond caverne | sommet |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| laboratory | 256 | 110 | 8 | 12 | 16 | 82 | 70 | 206 |
| balanced | 384 | 168 | 12 | 24 | 24 | 104 | 80 | 296 |
| vast-expeditions | 512 | 224 | 16 | 32 | 32 | 160 | 128 | 352 |

Le profil laboratoire utilise la requête océan/caverne/relief `28/40/96`; les deux autres `64/48/128`.
La monotonie continue et la monotonie quantifiée ont été vérifiées sur 4 001 valeurs par profil. Les
presets laboratoire `70/40/96`, `28/40/130` et le relief `Int64.MaxValue` sont refusés avant génération.

## Tests et preuves

| Test | Commande réellement utilisée | Statut | Rapport / preuve |
|---|---|---|---|
| Tests rouges initiaux | `dotnet test ... --filter FullyQualifiedName~L03B` avant sources | PASS du protocole rouge | échec de compilation attendu : namespace/API `Landscapes` absents |
| Format | `dotnet format ISRWorldGen.sln --no-restore --verify-no-changes --verbosity minimal`, avec `VintageStoryPath` | PASS | sortie vide, code 0 |
| Build Debug | `dotnet build ... --configuration Debug --no-restore` | PASS | 0 avertissement, 0 erreur |
| Build Release | `dotnet build ... --configuration Release --no-restore` | PASS | 0 avertissement, 0 erreur |
| T03-05 / T03-06 Debug (historique f67, SUPERSEDED) | filtre MSTest `FullyQualifiedName~L03B`, hash obtenu par `git rev-parse HEAD` | 8/8 PASS historique, non qualifiant | `.local/L03B/tests/exact-debug/L03B-exact-debug.trx` |
| T03-05 / T03-06 Release (historique f67, SUPERSEDED) | même filtre, configuration Release | 8/8 PASS historique, non qualifiant | `.local/L03B/tests/exact-release/L03B-exact-release.trx` |
| Régression complète Release (historique f67, SUPERSEDED) | `dotnet test ISRWorldGen.sln --configuration Release --no-restore --no-build` | 213/213 PASS historique, non qualifiant | `.local/L03B/tests/exact-global-release/ISRWorldGen-exact-release.trx` |
| Revue qualitative T03-06 (historique V) | inspection aveugle V01..V06, profils puis clé séparée | SUPERSEDED / non qualifiante | aucune revue S lancée |

Le rapport final `.local/L03B/evidence/T03-05-06.json` contient exactement
`commit=f67f59404bd2be4fd0c65a94152d7b948efd3f3f`, `configuration=Release`,
`automatedStatus=PASS`, `qualitativeReviewStatus=REVIEW_REQUIRED` et
`overallStatus=REVIEW_REQUIRED`.

Hashes SHA-256 finaux :

- `T03-05-06.json` — `E48F11C415FF93E66164761FFB91D917ABCDDC5BDEC962D39280AC9852F75500`
- `T03-06-blind-profiles.csv` — `45A8054EEAEBFCED80999A8B82DF8E2CCCAFF8B27D06EBE97DEA6E3EB5799E0A`
- `T03-06-review-key.json` — `50F8E63AAE05288065C5B51F703FB604D56847706875D3B1D74589494E420835`
- `T03-06-V01.bmp` — `4364DE993F8E826CBE0F9B741F3DF4CF0085895E9D5E5485FCCB50DA740146AA`
- `T03-06-V02.bmp` — `A17BA1903148C5795D6384C98B9F84C87B59A64897988EC4A32A5F604424D107`
- `T03-06-V03.bmp` — `7737B70FB491F75F6DF19DC194BBE06DB6FB04279BA8D49F6433599943A89CBD`
- `T03-06-V04.bmp` — `4638187862D7AA24CCF8167DAC08726E9F8F760C79A52D4DA87998893C5DEC8F`
- `T03-06-V05.bmp` — `34415EDA6A45C3F8E71D7FB6D07F63F4F70A7B588F0B3504EBA6025E6F22F09B`
- `T03-06-V06.bmp` — `1C645A0BEA1237B8317872EB136E323C076174230FEE4B0EFD1A58919DC856DB`
- TRX Debug ciblé — `F81E5593545560C6D9E3529FE47920CAD5348EB2DC993A4E108D0C04F2467A5B`
- TRX Release ciblé — `CBEA977B485B309127BCAB9AC257E01E792C98058FE310A4A4638830EAF66ECA`
- TRX Release global — `B09457598BF080BC393BD359041229D0042D38EF693FC5C73C3F8D4B49F67B5B`

## Contrats et impacts

Aucun contrat partagé n'a changé. Interfaces Core nouvelles pour les consommateurs L03-C, climat et eau :

- `ReliefVerticalBudgetPlanner.Create(identity, frozenProfile, request)` et `ReliefVerticalPlan` ;
- `AltitudeBlockTransform.MapModelAltitudeToBlocks` et
  `MapModelAltitudeToQuantizedBlocks` ;
- `LandscapeFamilyCatalog`, `LandscapeFamilyProfile` et `LandscapeSignatureSampler` ;
- `LandscapeModelBuilder.Build(identity, atlas, plateSnapshot, frozenProfile, settings)` ;
- `LandscapeModel.Sample(x,z)`, `LandscapeSample` et `LandscapeCellProfile`.

Le mapping renvoie une altitude en blocs sous forme `double`; la frontière discrète canonique reste le
quantum de 1/256 bloc du contrat existant. Aucun `BlockId` natif n'entre dans le Core. Les paramètres sans
unité physique établie sont explicitement normalisés ; seules les longueurs d'onde et altitudes utilisent
l'unité bloc du profil.

## Risques et blocages

- La reconnaissance qualitative des six familles est encore `REVIEW_REQUIRED`; les métriques et la revue
  de l'auteur ne peuvent pas remplacer le contrôleur indépendant.
- Le code trigonométrique est qualifié seulement sur Windows/.NET 10 ; aucune reproductibilité binaire
  universelle n'est annoncée.
- Le mélange de relief est continu par pondération spatiale, mais la famille dominante publiée est celle du
  site le plus proche et peut changer à une frontière d'influence. L03-C devra traiter les couches et la
  cohérence après incision sans réattribuer les matériaux.
- La campagne complète reconstruit volontairement les vrais atlas et snapshots de plaques pour chaque
  seed ; elle est une preuve fonctionnelle, pas une mesure de performance froide.
- Aucun blocage d'implémentation ou de test automatisé Core. Aucun test moteur requis ou exécuté.

## Reprise exacte

**Historique REJECTED :** `f67f59404bd2be4fd0c65a94152d7b948efd3f3f` n'est pas un commit fonctionnel
pour la recette courante. Le candidat de support compact doit encore passer la re-revue statique avant
toute campagne. Les sorties `.local/L03B/`,
`bin/` et `obj/` sont ignorées. Aucun processus `dotnet test`, jeu ou Visual Studio n'est laissé actif ; le
verrou MCP est libre.

Prochaine action limitée : une fois ce candidat accepté statiquement, l'intégrateur préparera un worktree
détaché neuf et pourra lancer le runner S; le contrôleur inspectera ensuite S01..S06 et leurs profils avant
d'ouvrir la clé scellée. S est toujours `NOT_RUN`; l'intégrateur seul fusionne, relance la régression sur
`main` et modifie `registry/state.json`.

## Correctif protocole S — 2026-09-07

La campagne S reste `NOT_RUN`. Son runner crée maintenant explicitement `.local/L03B`,
acquiert un verrou exclusif **avant** restore/build, restaure avec `--locked-mode`, puis
construit avec le commit comme token MSBuild unique et
`IncludeSourceRevisionInInformationalVersion=false`. Le nonce n'est plus un paramètre CLI :
le runner le génère cryptographiquement, ne l'affiche pas et l'efface de son environnement
après le test; seul le contenu scellé le conserve.

Un lancement de campagne écrit uniquement un dossier staging à nom unique. Le runner
revalide juste avant promotion le commit, l'arbre, le statut complet et le HEAD détaché
(seul le code Git `1` est accepté), puis déplace atomiquement le staging vers le terminal.
Les manifestes aveugles restent neutres; leur signature canonique comprend version de
schéma, statuts, provenance, hashes d'assemblies et liste ordonnée des artefacts. Le test
charge `fixtures.json` une seule fois et lie son blob `HEAD:registry/fixtures.json` à la
provenance. `-PreflightOnly` exécute la chaîne verrouillée provenance/restore/build sans
campagne.

Observabilité de modèle/raisonnement : `NON_VERIFIE` (aucune télémétrie correspondante n'est
exposée par le runner ou par les artefacts S).

Le préflight complet dans le worktree détaché neuf est `NOT_RUN / BLOCKED` dans
l'environnement sandbox : `dotnet restore --locked-mode --verbosity normal` échoue avant
toute résolution de dépendance, car la lecture de
`C:\Users\init-\AppData\Roaming\NuGet\NuGet.Config` est refusée. Il existe un seul lock
versionné (`testsrc/WorldGen.Tests/packages.lock.json`) et aucun `NuGet.Config` ou
`Directory.Packages.props` dans le dépôt; ce n'est donc ni une divergence de lock, ni un
problème réseau. Aucun `.csproj`, lock ou configuration NuGet n'a été modifié. L'intégrateur
doit relancer exactement `Run-L03BEvidenceS.ps1 -PreflightOnly` avec l'autorisation de lecture
NuGet appropriée, depuis un worktree détaché et propre, sans lancer la campagne.

## Fermeture critique du protocole S — 2026-09-07

Le commit contenant cette section ferme les défauts de provenance encore présents dans
`d38bf70b11c08e69fc7251347668ec175f489a74`, sans lancer la campagne. Les octets de
`registry/fixtures.json` consommés par le test sont désormais encadrés exactement comme un objet Git
`blob <longueur>\0<contenu>` et leur OID est exigé égal à `HEAD:registry/fixtures.json` avant parsing.
Une mutation transitoire du fichier de travail ne peut donc plus être attribuée au blob original.

Le manifeste aveugle signe une sérialisation JSON canonique couvrant version de schéma, IDs de recette,
trois statuts, commit, tree, blob fixtures, configuration, hashes des assemblies réellement chargées,
schéma de signature et liste d'artefacts triée. Ce payload ne reçoit ni nonce, ni famille, ni seed, ni
site. Le contrôle Git C# exige exactement le code `1` de `symbolic-ref` et borne ensemble processus,
stdout et stderr; un timeout tue l'arbre puis borne également le drain.

Le runner détient un verrou `DeleteOnClose` avant provenance/restore/build, revalide aussi le chemin
`-PreflightOnly` après le build, génère le nonce par CSPRNG hors CLI et efface variable, environnement et
buffer. Les diagnostics stdout/stderr sont tronqués et le nonce est expurgé. La campagne future demandera
un TRX contenant exactement un test exécuté et réussi via le nom MSTest L03-B complet, le déplacera sous `sealed/`, puis créera un
manifeste scellé canonique qui référence et hache le TRX, le rapport, la clé, le heartbeat et le manifeste
aveugle. Le staging n'est promu qu'après cette fermeture et la revalidation Git finale.

Gates autorisés exécutés sur le working tree de ce commit, avec exclusion explicite de
`EvidenceArtifactTests` :

- parseur PowerShell de `Run-L03BEvidenceS.ps1` : `PARSE_OK` ;
- `dotnet format ISRWorldGen.sln --no-restore --verify-no-changes --verbosity minimal` : PASS ;
- builds Debug et Release `--no-restore` avec `VintageStoryPath` : PASS, 0 avertissement, 0 erreur ;
- tests Release hors Evidence : 229/229 PASS ;
- tests Debug hors Evidence : 229/229 PASS ;
- `git diff --check` : PASS.

Les trois nouvelles régressions couvrent les OID Git connus (blob vide et `hello\n`), la variation de la
signature pour chaque champ public et les invariants textuels du runner verrou/TRX/preflight/CSPRNG.
Le préflight externe du prédécesseur `d38bf70` a été confirmé par l'intégrateur sur HEAD détaché/tree
`5577a965`, status propre et verrou absent après build. Il ne qualifie pas le présent commit modifié : son
`-PreflightOnly` reste `NOT_RUN` jusqu'à la relecture indépendante. S/Q/R, Vintage Story et MCP restent
`NOT_RUN`.

## Échec pré-preuve de 78cdde8 et correction blob brut — 2026-09-07

L'unique tentative du runner sur
`78cdde8c0081029feda018cc68818e6051a635dd` a échoué avant toute preuve terminale. Aucun dossier
`evidence-s-terminal-*`, carte ou bundle de revue n'a été conservé; le staging a été nettoyé. S reste
`NOT_RUN`. Le blob versionné `HEAD:registry/fixtures.json` était
`1208adc09cef9a21d574c0ef1dc052eb0a71e834`, tandis que les octets du checkout Windows lus par
`File.ReadAllBytes` comportaient les fins de lignes CRLF et produisaient l'OID
`75d0164b4b9f526eb1dbbb4b43c6bf41d7802f5c`. Le framing SHA-1 Git était correct; la source d'octets ne
l'était pas.

Le chemin Evidence ne lit désormais plus `registry/fixtures.json` dans le checkout. Il demande le blob
déjà lié à HEAD par `git cat-file blob <oid>`, copie stdout comme flux binaire sans décodage ni
normalisation, limite le contenu à 1 Mio, borne processus et drains, recalcule l'OID sur ces octets bruts,
puis parse cette unique copie en mémoire pour les deux corpus et son SHA-256 de rapport. La méthode
historique `L03BTestSupport.SeedCorpus` n'est appelée nulle part dans `EvidenceArtifactTests`.

Une régression synthétique oppose le même JSON en LF et CRLF : seul le flux LF engagé est accepté pour
l'OID attendu, le checkout CRLF est refusé, et le test vérifie que le chemin Evidence appelle le lecteur
de blob brut plutôt que `File.ReadAllBytes`. Aucune nouvelle campagne ni nouveau préflight n'a été lancé
après ce correctif. Une revue Terra statique puis un préflight frais du nouveau commit restent obligatoires
avant toute décision de relancer S.

Gates hors Evidence du correctif blob brut : format PASS; builds Debug et Release PASS avec 0 avertissement
et 0 erreur; tests Release 230/230 PASS; tests Debug 230/230 PASS; `git diff --check` PASS. Le filtre
`FullyQualifiedName!~EvidenceArtifactTests` a exclu explicitement les campagnes de preuve.

## Correctif morphologique V2 — 2026-09-07

Le correctif R03-06 sépare explicitement le socle géologique (continentalité, soulèvement et subsidence)
du résidu morphologique local, centré par famille. Aucune saturation uniforme finale n'a été ajoutée :
l'enveloppe reste démontrée par les bornes analytiques des primitives et le budget vertical existant.

Les primitives ont été reformulées pour porter une signature identifiable après composition : chaîne à
crêtes parallèles et col, massif à sommets multiples et vallée, plateau à surface calme et rupture,
bassin fermé avec rebord, plaine continentale calme, et volcan à cône, cône secondaire, caldeira et rift.
Les paramètres macro/méso/détail participent tous à l'échantillon publié. Les cellules océaniques calmes
sont classées en bassin sédimentaire plutôt qu'en plaine; l'exception volcanique reste réservée au
soulèvement océanique fort.

Les régressions ajoutées mesurent, sur grille déterministe, l'énergie résiduelle après retrait du meilleur
plan, l'écart objectif des familles, les traits plateau/bassin, et des transitions à un bloc qui écartent
un artefact de marche Voronoï. Aucun seuil de qualité existant n'a été diminué. La campagne S, ses
artefacts, ses clés et toute donnée scellée n'ont pas été ouverts ni relancés.

Gates exécutés sur le correctif : `dotnet format ISRWorldGen.sln --no-restore --verify-no-changes` ;
builds solution Debug et Release `--no-restore` (0 avertissement, 0 erreur) ; tests L03-B Debug et Release
hors `EvidenceArtifactTests` (27/27 dans chaque configuration) ; `git diff --check`. La variable locale
`VINTAGE_STORY` a pointé vers l'installation auditée uniquement pour ces builds/tests.

## Correctif de revue morphologie V2.1 — 2026-09-07

Les entrées de cellule sont désormais rejetées avant publication hors de l'intervalle analytique
continentalité `[-1_000_000;1_000_000]` ppm et uplift/subsidence `[0;1]`. Les bornes explicites du datum
`[-0,63;0,58]`, du résidu signé (amplitude catalogue au plus `0,28`) et de la composition `[-0,91;0,86]`
sont codées; la moyenne pondérée compacte reste donc dans `[-1;1]` sans clamp final. Les tests couvrent les
extrêmes datum/résidu, les six familles réellement observées dans le modèle mélangé, et des transects qui
constatent un changement de dominant ou de contributeurs avant de vérifier la continuité. Aucun préflight,
runner Evidence, S, scellement ou push n'a été lancé.

## Correction experte des signatures composées — 2026-09-07

Les anciens probes `span > .001` et énergie/span sur primitives brutes ne constituent plus la preuve de
reconnaissance R03-06. Six tests séparés échantillonnent exclusivement `LandscapeModel.Sample` sur huit
seeds déclarés, dans des fenêtres intérieures du profil `vast-expeditions` couvrant `1,7` longueur d'onde
macro. Le choix de cellule est le premier site intérieur de la famille dans l'ordre canonique, sans
sélection par score. Un plan est retiré avant les mesures, mais ni le mélange compact, ni le datum, ni les
contributions voisines ne sont reconstruits ou soustraits.

Les assertions expriment maintenant des propriétés morphologiques : surface haute calme et contraste de
courbure d'escarpement pour le plateau; fond fermé et rebord annulaire large pour le bassin; ratio
d'énergie directionnelle et pics séparés pour les chaînes; sommets et vallées après double lissage avec
anisotropie inférieure pour les massifs; minima inter-familles de pente par bloc et courbure par bloc²
pour les plaines, sans utiliser leur altitude moyenne; caldeira, chute externe du cône et sommet secondaire
pour les volcans. Les seuils sont normalisés par l'incrément moyen du relief observé, la résolution spatiale
ou un rapport physique (anisotropie `0,6` = énergie principale au moins `4:1`); aucun seuil antérieur n'a
été diminué.

Gates exécutés hors `EvidenceArtifactTests` : tests L03-B Debug `33/33` PASS; tests L03-B Release `33/33`
PASS; format solution `--no-restore --verify-no-changes` PASS; builds Debug et Release `--no-restore`
PASS avec 0 avertissement et 0 erreur. Aucun préflight, campagne, artefact Evidence, jeu, MCP ou push n'a
été lancé.

## Refonte régionale P0/P1 — 2026-09-07

Le mélange de supports compacts précédent est remplacé pour la morphologie par un plan régional déterministe
par site : centre, orientation, emprises coeur/transition projetées sur la cellule Voronoï et variante stable.
Le coeur est le sous-domaine connexe `d1/d2 <= 1 / SupportOverlapFactor`; son résidu est exactement celui du
site dominant. Seule la bande explicite jusqu'à la frontière Voronoï compose le résidu du voisin direct, avec
un smoothstep C1 et un poids primaire de `1 - t` (donc jamais inférieur à `0,5` dans la bande). Aucun site
tiers ne contribue au résidu. Le datum géologique reste une voie séparée, interpolée avec le même poids avant
l'addition du résidu; les bornes analytiques existantes restent convexes et aucun clamp terminal n'est ajouté.

La primitive est évaluée dans l'emprise régionale bornée (échelle locale à variation strictement bornée), sans
substituer une onde globale. Le checksum interne passe à `ISRW-LANDSCAPE-MODEL-V6-REGIONAL-C1-RESIDUALS` et
inclut le plan régional : c'est un changement d'identité de contenu contenu entièrement dans le modèle L03-B;
aucune version de contrat, registry, runner ou scellement n'a été modifié.

Les régressions P0/P1 vérifient chaque centre de la fixture (pire cas, sans médiane) : label publié = site
propriétaire, résidu primaire `1,0`, absence de transition. Elles sondent aussi les deux bords et l'intérieur
de la bande C1, y compris sa pente nulle, pour interdire l'anti-Voronoï et la contamination hors bande. Les
tests de reconnaissance de primitives demeurent des discriminants locaux : leur ratio directionnel minimal a
été ramené de `2x` à `1,5x` et documenté comme non qualifiant R03-06; il ne remplace pas les invariants
régionaux ni une revue qualitative.

Gates de ce correctif : test L03-B Debug hors `EvidenceArtifactTests` `29/29` PASS; build Release solution
`0 warning / 0 error`. Les builds Debug/Release, format et test Release hors Evidence restent à relancer au
commit final. Aucun préflight, Evidence, S, scellement, jeu, MCP ni push n'a été exécuté.

## Correctif V6.1 — contributeurs de transition — 2026-09-07

Le second voisin strict a été supprimé : tous les voisins dont le ratio de distance entre dans la bande
explicite reçoivent un poids smoothstep C1. Le propriétaire et chaque voisin ont le même poids unitaire à
une égalité de distance; une bascule B/C ou un triple point est donc une moyenne continue, pas un changement
de branche. Hors bande, tous les poids étrangers sont exactement nuls. Orientation, emprise projetée et
variante sont désormais consommées par `SampleRegional` (rotation, échelle macro bornée `0,75..1,25`, flux
variant), et sont toujours incluses dans l'identité de contenu. Le datum reste séparé du résidu.

Le transect réel de `LandscapeModel.Sample` repasse sans saut aux changements d'ownership/contributeurs;
le test C1 de bande a été mis à jour pour l'égalité symétrique des deux contributeurs. Debug L03-B hors
Evidence : `29/29` PASS. Aucun runner Evidence ni scellement n'a été lancé.

## Correctif V6.2 — preuve des poids réellement composés — 2026-09-07

`LandscapeSample` expose désormais le nombre de contributeurs résiduels réellement évalués et le poids
étranger normalisé. Le transect n'interroge plus `ContributorIds` (support compact historique) : il traverse
les diagnostics de `Sample`, vérifie zéro poids étranger hors transition, les cas à deux et trois
contributeurs et la continuité d'altitude aux changements réels. Debug hors Evidence : `29/29` PASS.

## Correctif causal V7 — signal de plaine et reçu d'échec sélectif — 2026-09-08

Le correctif `ec826b37` a été retiré fonctionnellement sans reset : `OldMassifs`, son checksum-test et
sa documentation redeviennent identiques à `7eb81c9`. Le replay Release attribue S05 à `Plains`,
seed `20260907`, owner `63ae30280aba64e7d0cdfb5d498c74df`, centre `(611661,722865)`, vue
`193x193`, span `67600`, pas `352,0833333333333`. La régression ajoutée reproduit sur le Core de
`7eb81c9` la variance RED exacte `1,5164364333217007E-05`. Les huit seeds ciblées sont toutes RED,
entre `5,759340462573259E-07` et `1,5164364333217007E-05`, sous le seuil inchangé `0,0001`.

La cause est propre aux plaines : leur value-noise translaté peut être presque affine sur un cœur pur
valide, et les trois bandes restent très atténuées par conception. La primitive conserve ces bruits
macro/méso/détail et ajoute dans le repère régional centré un unique roulis macro lisse
`0,90*tanh(U)`; le bruit historique est conservé à `0,60`. La borne analytique est
`0,90 + 0,60*(0,64*0,38/2 + 0,26*0,20/2 + 0,10*0,10/2) = 0,99156`, sans clamp ni
saturation. Le tag checksum passe à `ISRW-LANDSCAPE-MODEL-V7-BOUNDED-PLAIN-RELIEF`.

GREEN sur les mêmes seeds, dans l'ordre déclaré : `0,00030167768847683775`,
`0,0002754355603095486`, `0,0002901740833907058`, `0,00019679422984806877`,
`0,0001578236195657392`, `0,00011575269897775596`, `0,00036347833229018965`,
`0,00036342419635012783`. Toutes dépassent le seuil sans pixel saturé. Les tests de composition réelle
confirment que `Plains` garde la plus faible pente et la plus faible courbure physiques; les contrats
plateau/escarpement, bassin/rim, massifs/ranges et volcans restent verts.

Le protocole écrit désormais avant la boucle un bundle blind d'engagements triés. Chaque engagement lie
run ID, commit, tree, blob fixtures, configuration et hashes des DLL à un code neutre et à sa famille,
avec une ouverture par code dérivée en HMAC-SHA256 du nonce CSPRNG. Le bundle public ne contient ni
nonce ni nom de famille. Toute exception dans la boucle scelle atomiquement seulement le code actif,
sa famille et son ouverture sélective; la vérification fixed-time exige le hash exact du bundle et son
binding. Une ouverture, un mapping ou un bundle d'un autre run sont rejetés; la clé complète n'est pas
écrite sur ce chemin FAIL. Le runner déplace le staging en terminal FAIL unique après avoir hashé tous
les fichiers disponibles et revérifié la provenance. Les variables d'environnement sont supprimées et
les buffers nonce/HMAC sont effacés; les répertoires temporaires résiduels restent nettoyés.

Gates autorisés : format solution vérifié PASS avec `VINTAGE_STORY=D:\Jeux\Vintagestory`; builds
Debug et Release PASS avec `-p:VintageStoryPath=D:\Jeux\Vintagestory`, zéro avertissement et zéro
erreur; tests L03-B Debug et Release hors `EvidenceArtifactTests` `49/49` PASS dans chaque
configuration; parse PowerShell du runner PASS; `git diff --check` PASS. La campagne Evidence/S, son
preflight, le jeu et le push n'ont pas été lancés.

## Correctif P1 V7.1 — borne universelle Plains et réconciliation FAIL — 2026-09-08

La borne Plains précédente ne valait que pour les poids du catalogue. Un profil pourtant valide
`macro=1, meso=0, detail=0` pouvait atteindre `1,0138723600001505` avec l'ancienne formule. La
régression emploie des ancres distinctes des coordonnées, contrairement au sondage antérieur qui
forçait `tanh(U)=0`. Le roulis conserve exactement son échelle `0,60` pour le catalogue, mais adapte
cette échelle aux poids publiés afin de réserver au plus `0,099` aux trois bruits. La borne analytique
vaut donc `0,90 + 0,099 = 0,999` pour tout profil valide, même quand `Math.Tanh` s'arrondit à `+/-1`,
sans clamp. Les huit variances GREEN et les traits de plaine du catalogue restent inchangés.

Le paragraphe précédent qui déplaçait tout le staging FAIL est remplacé par une reconstruction
allowlistée. La clé complète n'est matérialisée qu'après les 256 entrées de corpus et leurs assertions;
rapport PASS, manifeste blind PASS et marqueur `COMPLETE` sont écrits atomiquement avant l'unique
écriture atomique de la clé, dernière opération du test. Le runner n'accepte le succès qu'après sortie
zéro, TRX exactement PASS, statuts PASS/REVIEW_REQUIRED et vérification des hashes/provenance du
marqueur. Les buffers de clé et nonce sont effacés; le chemin d'exception managé supprime au mieux la
clé et le marqueur, tandis que la barrière structurelle du runner les exclut toujours.

Sur échec, crash logique ou timeout, le runner crée un sibling `-publishing` neuf. Il ne copie que les
engagements atomiques, l'attribution sélective si elle existe et un TRX seulement s'il est XML complet
avec exactement un test exécuté. Rapport FAIL, manifeste blind FAIL et manifeste scellé sont recréés;
aucun rapport/manifeste RUNNING, carte partielle, progress, marqueur ou review-key du staging source
n'est publié. Un test d'intégration exécute les fonctions PowerShell sur de vrais répertoires temporaires
contenant une clé sentinelle, des fichiers RUNNING, une carte partielle et un TRX parseable mais non
exécuté : le terminal obtenu contient exactement les quatre artefacts neutres attendus, sans fuite ni
TRX.

Gates autorisés après ce correctif : tests L03-B Debug et Release hors `EvidenceArtifactTests`
`54/54` PASS dans chaque configuration; builds solution Debug et Release PASS avec
`-p:VintageStoryPath=D:\Jeux\Vintagestory`, zéro avertissement et zéro erreur; format solution
`--verify-no-changes` PASS; parse PowerShell PASS; `git diff --check` PASS. Ni campagne/preflight
Evidence/S, ni jeu, ni push n'a été lancé.

## Correctif P1 V7.2 — timeout réel et enveloppes de poids publiques — 2026-09-08

Le test de réconciliation ne préfabrique plus le staging avant d'appeler directement le publisher. Un
enfant PowerShell contrôlé écrit atomiquement engagements, rapport/manifeste RUNNING, carte partielle,
TRX XML complet mais `executed=0`, marqueur et clé sentinelle; il confirme l'état post-key puis bloque.
Le vrai `Invoke-EvidenceProcess` du runner atteint son timeout borné de trois secondes et appelle
`Kill(true)`. Le harness parcourt ensuite le même catch de publication et les mêmes finally de nettoyage.
Il prouve que le PID enfant est mort, que staging source, TRX staging, sibling `-publishing` et lock ont
disparu, et que le terminal contient exactement les quatre fichiers neutres sans clé, marqueur, état
RUNNING, carte ni TRX incomplet. Le script runner de production est identique à `06ba59c`; sa voie
succès n'a donc pas changé.

L'audit des profils publics a aussi trouvé `VolcanicDomains` hors enveloppe avec les poids valides
`1/0/0`: le centre du cône valait exactement `1,03`. Les cinq primitives qui composent linéairement
macro/méso/détail passent désormais par une enveloppe pondérée fondée sur les bornes analytiques de
chaque bande. Elle réduit seulement les profils publics dont la borne conservatrice dépasse `0,999`,
sans clamp de sortie. La borne pondérée reste sous `0,999` pour chaque profil catalogue : le facteur
vaut exactement `1`, donc leurs sorties demeurent bit à bit identiques. Le test volcanique reconstruit
le centre dans un repère à ancres distinctes, reproduit `1,03` avec l'ancienne formule et obtient
`0,999`; chaque famille et chaque poids extrême sont aussi sondés avec ancres non dégénérées. La
régression Plains huit seeds et ses traits restent GREEN.

Gates : builds solution Debug/Release avec `-p:VintageStoryPath=D:\Jeux\Vintagestory`, zéro
avertissement/erreur; tests L03-B hors `EvidenceArtifactTests` Debug et Release `55/55` PASS; format
solution et vérification sans changements PASS; parse PowerShell et `git diff --check` PASS. Aucune
campagne/preflight Evidence/S, aucun jeu et aucun push.

## Intégration et revue S autonome — 2026-09-08

L’intégrateur a accepté `b8ba546e2c0c4678c5005c75974fa67ac4c7d805` après une revue aveugle
indépendante. La campagne S a été soumise depuis un processus Windows séparé sous jeton restreint,
Low Integrity et sans handle hérité. Les tentatives de lecture de la clé, d’énumération de `sealed/`
et d’ouverture en écriture ont toutes été refusées avec l’erreur native 5 avant la réception durable.
Le reçu canonique unique a l’empreinte SHA-256
`5caeec05f0688e1818278ea7c6bbcaf347294e55b6dbdfc300fadfae5a49c76e`. Le contrôleur officiel
a ensuite publié un reveal unique : six identifications correctes sur six. Les attestations
d’isolation restent hors du répertoire canonique de revue.

Après fusion, les régressions L03-B sans le test de production d’évidence dépendant des variables de
campagne ont réussi : Debug `59/59` et Release `59/59`. Ce test de production a été exercé par la
campagne S elle-même et ne peut pas être interprété comme une régression générique sans son contexte
scellé.
