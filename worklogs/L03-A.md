# Passation — L03-A

**Statut proposé : REVIEW.** Le sous-agent ne déclare pas `DONE`.

## Identité

- Tâche : L03-A — Produire plaques et masses continentales.
- Propriétaire : `agent:l03_a`.
- Branche / worktree : `agent/l03-a` / `E:\Développement\Vintage Story\ISRWorldGen-worktrees\l03-a`.
- Base publiée obligatoire : `94ec68b89256317499191437113d0fe72359e29a` (`origin/main`).
- Commit fonctionnel final vérifié : `01dcbfb6cde5736c754d440ea63a99194d81e42a`.
- Commit de passation : commit contenant ce fichier ; son hash exact est fourni dans le message de remise, car un fichier ne peut pas contenir le hash de son propre commit.
- Contrats lus et inchangés : `C01 v1.0`, `S03 v1.0`, `T03`.
- Prérequis vérifié : `registry/state.json` indiquait `L02-C = DONE` avant édition.
- Runtime qualifié : .NET SDK `10.0.400`, runtime `10.0.11`, cible `net10.0`, Windows.
- Jeu / MCP / sauvegarde : `NOT_RUN`, non requis par T03-01/T03-02 ; aucune instance Vintage Story ni sauvegarde ouverte.

## Résultat

R03-01 est implémentée par une cinématique bornée en unités normalisées. La convention publique est `delta-v = vB - vA` avec une normale orientée A vers B : produit scalaire négatif pour la collision, positif pour la divergence, composante normale nulle et mouvement tangentiel pour le cisaillement. La permutation A/B accompagnée de l'inversion de la normale conserve classification, champs scalaires et checksum. Les vitesses non finies ou de norme supérieure à un et les normales nulles/non finies sont refusées.

Le snapshot de plaques est construit depuis l'`AtlasMesh` immuable de L02. Les cellules portent l'âge et le type de croûte ; une même plaque peut agréger plusieurs types de croûte. Les champs d'uplift et de subsidence combinent des enveloppes locales et régionales bornées, avec coupure laissant des intérieurs calmes.

R03-02 est implémentée par un modèle continental global immuable, échantillonné en coordonnées monde. Cinq enveloppes macro et dix-huit structures régionales elliptiques déformées créent plusieurs échelles sans employer les cellules Voronoï comme paysages. Diviser la même série de coordonnées entre plusieurs fenêtres conserve exactement les valeurs. L'analyse topologique extrait aires et composantes terrestres, océans connectés au bord, bassins intérieurs, segments de contour exacts en coordonnées rationnelles, changements de direction et plus longue portion rectiligne.

Le correctif de qualification lie désormais explicitement modèle continental, atlas de plaques et identité de génération à un `FrozenScaleProfile` L02-C. Le builder vérifie le hash de configuration, le domaine réel et le quota de sites du profil. Le snapshot publie l'identifiant/version de profil, la résolution d'atlas et la taille de tuile, qui participent à son checksum.

Le coût de propagation est préflighté avant construction continentale et avant les dictionnaires/champs : la borne conservatrice `atlas.Edges.Count * atlas.Cells.Count` est comparée à `MaximumBoundaryInfluenceEvaluations` avec arithmétique `BigInteger`. Un dépassement retourne `BudgetExceeded` au stage `geology.plates.influence-budget`. Le test mesure moins de 64 KiB alloués sur ce chemin de refus après préchauffage, ce qui protège le refus avant le calcul quadratique.

Fichiers modifiés :

- `src/WorldGen.Core/Geology/Plates/PlateBoundaryKinematics.cs`
- `src/WorldGen.Core/Geology/Plates/PlateAtlas.cs`
- `src/WorldGen.Core/Geology/Plates/ContinentalField.cs`
- `src/WorldGen.Core/Geology/Plates/ContinentalTopology.cs`
- `testsrc/WorldGen.Tests/L03A/L03ATestSupport.cs`
- `testsrc/WorldGen.Tests/L03A/PlateBoundaryTests.cs`
- `testsrc/WorldGen.Tests/L03A/PlateAtlasBuilderTests.cs`
- `testsrc/WorldGen.Tests/L03A/ContinentalFieldTests.cs`
- `testsrc/WorldGen.Tests/L03A/EvidenceArtifactTests.cs`
- `worklogs/L03-A.md`

Aucun stub, aucune dépendance Vintage/GPU, aucune modification de `.csproj`, solution, NuGet, contrat, registre, plan, version ou checksum partagé.

## Campagne et politique de revue

Le corpus figé de `registry/fixtures.json` est exécuté sans renommage synthétique :

- 192 seeds de calibration avec le `FrozenScaleProfile balanced` réel : monde `131072 × 131072`, tuile `512`, résolution d'atlas `16384`, demande `64` sites, quota `128`, hash de géographie `ef78edea8c25b2cf03e5080ca7a154c50344eb1a8495c910df5bf1a6ce4de76c` ;
- 64 seeds holdout avec le `FrozenScaleProfile vast-expeditions` réel : monde `1024000 × 1024000`, tuile `2048`, résolution d'atlas `90000`, demande `128` sites, quota `256`, hash de géographie `f53eaa2f665507ce184075a2435aefdf86beba937ddb68d6252275ac934cfb21`.

Chaque cas est créé via l'overload `ContinentalFieldModel.Create(identity, frozenProfile, settings)`. Sa grille 96 × 96 couvre environ 99 % de chaque axe et traverse deux frontières réelles de tuile du profil ; la campagne mesure donc les alignements en blocs du monde et non en indices de tableau. Le maximum observé est 52 529 ppm d'alignement pour `balanced` et 47 882 ppm pour `vast-expeditions`, sous le plafond déclaré de 20 %.

T03-02 ne donne aucun seuil numérique normatif. La politique ci-dessous a été déclarée avant la campagne finale et reste une alarme de projet, pas une preuve qualitative autonome ni un oracle scientifique :

- `75 %`, soit 192/256 cas : supermajorité choisie pour exiger un comportement dominant tout en tolérant jusqu'à un quart de cas atypiques ; le résultat observé est 256/256. Le cas compte si au moins 5 % de ses cellules côtières changent de direction.
- `20 %` : plafond de dominance commun à une portion droite unique, à l'alignement atlas et à l'alignement tuile. Un cinquième signale une structure systématique visible sans exiger un bruit artificiel partout.
- `299` et `751` échantillons : sur une carte 96 × 96 (9 216 échantillons), ces témoins bornent respectivement une forme régionale à au plus 3,25 % et une masse continentale à au moins 8,15 %. L'intervalle entre les deux évite qu'un seul gabarit médian satisfasse les deux classes.
- `8` restes distincts modulo 32 : alarme faible exigeant au moins un quart des résidus possibles contre des aires quantifiées. Elle complète, sans la remplacer, la vérification géométrique des vraies frontières de tuiles L02-C.

`PatternDiagnostics` n'est pas utilisé comme oracle unique. Le verdict automatique combine déterminisme, comptages, connectivité, contours, diversité d'aires et alignements ; la revue des cartes hashées reste indépendante.

## Publication sûre de la preuve

Le rapport `.local/L03A/evidence/T03-01-02.json` ne peut plus conserver un ancien `PASS` après une campagne échouée :

1. le rapport précédent est remplacé atomiquement par `RUNNING` avant la campagne ;
2. toute exception remplace atomiquement le rapport par `FAIL` et relance l'échec de test ;
3. calculs, assertions et écritures d'artefacts sont terminés avant la sérialisation finale ;
4. l'écriture atomique du rapport avec `automatedStatus=PASS` est la toute dernière opération.

Même après succès automatisé, `qualitativeReviewStatus` et `overallStatus` restent `REVIEW_REQUIRED`.

## Tests et preuves

| Test | Commande ou capacité réellement utilisée | Statut | Rapport / preuve |
|---|---|---|---|
| Format | `dotnet format ISRWorldGen.sln --no-restore --verify-no-changes --verbosity minimal`, avec `VintageStoryPath` défini | PASS | sortie vide, code 0 |
| Build Debug | `dotnet build ISRWorldGen.sln --configuration Debug --no-restore -p:VintageStoryPath=...` | PASS | 0 avertissement, 0 erreur |
| Build Release | `dotnet build ISRWorldGen.sln --configuration Release --no-restore -p:VintageStoryPath=...` | PASS | 0 avertissement, 0 erreur |
| T03-01 / T03-02 Debug | filtre MSTest `FullyQualifiedName~L03A`, `--no-build` | PASS 10/10 | `.local/L03A/tests/final-debug/L03A-final-debug.trx` |
| T03-01 / T03-02 Release | filtre MSTest `FullyQualifiedName~L03A`, `--no-build` | PASS 10/10 | `.local/L03A/tests/final-release/L03A-final-release.trx` |
| Régression complète Release | `dotnet test ISRWorldGen.sln --configuration Release --no-restore --no-build ...` | PASS 205/205 | `.local/L03A/tests/final-global-release/L03A-global-final-release.trx` |
| Revue visuelle locale | continent, région, domaines marins, plaques, croûte, champs et superposition atlas | inspection de cohérence seulement | la revue indépendante demeure `REVIEW_REQUIRED` |

Une tentative de `dotnet format` sans la variable `VintageStoryPath` a échoué sur la résolution des types de l'API Vintage Story. Aucun fichier n'a été modifié par cette tentative ; la relance avec le chemin qualifié a passé. Aucun test requis ne reste rouge.

Preuves finales, toutes générées en Release avec `commit=01dcbfb6cde5736c754d440ea63a99194d81e42a` :

- `.local/L03A/evidence/T03-01-02.json` — SHA-256 `E62807238E11E810E361192861C5706A6D3BCC2613085F16F5FC0E330069DFA9`.
- `.local/L03A/evidence/T03-02-continent.bmp` — `2E0A8C9BAC36017D4CE9DE6EDECFA1320DA6B1E5AC881B9CF78756D62C43CA00`.
- `.local/L03A/evidence/T03-02-region.bmp` — `A669226053DAF6CD4A775BFDD036A9ECCABD98E16EB9A0D95D2188F231FD1B53`.
- `.local/L03A/evidence/T03-02-marine-domains.bmp` — `D1A3EBB364BD880D2D7B2888AD13DE17CD8638326999274A402EB8A875844AE4`.
- `.local/L03A/evidence/T03-02-plates.bmp` — `8736CB154B005267A7A96B71E4E4311362D669813DB39EA589AF6DF50B49BBCC`.
- `.local/L03A/evidence/T03-02-crust.bmp` — `C1DAEC0D73545A0429B2215C230A768C7B92EA380E8520CA05D163D972490929`.
- `.local/L03A/evidence/T03-02-uplift-subsidence.bmp` — `92D977DB8CB919E4F92A41FFBC868F5FF2115854910D81E5B9D114FF68BC79A0`.
- `.local/L03A/evidence/T03-02-atlas-overlay.bmp` — `E483BA22D0702D4B03B2840BBF818A433DC7782F2F6E9597321328528DD09AE5`.
- `.local/L03A/evidence/T03-02-contours.csv` — `4341765D790F251BC15FD020E2EB131C747C449082ACB2599BF4D5341C5DF819`.
- TRX Debug — `BD2C8F23B07363D5932E2FCE57CD0A1204240455A69DEA27D714FDD56355FC6F`.
- TRX Release — `6023B4EC8648619887C0370C39ECA3F4929213C3FBA90900A2E71B3162DABEA8`.
- TRX global Release — `6BAB7DCA67F2DE30FF6FD446CEE379EA9918819CBEF5CF7BFE0378C109BAE61C`.

Métriques de la vue continent seed 73, profil `balanced`, 192 × 192 : aires terrestres `[126, 210, 272, 386, 15992]`, 976 transitions de littoral, 76 alignées avec une arête d'atlas (77 868 ppm), aucune alignée avec une frontière de stockage dans cette vue. Le zoom régional contient 197 segments de contour. Le témoin marin reproductible seed `-413555959`, profil `balanced` 96 × 96, distingue 4 531 échantillons d'océan connecté et 408 échantillons de bassin intérieur.

## Contrats et impacts

Aucun fichier de contrat partagé n'a changé. Interfaces Core locales consommables par L03-B :

- cinématique : `PlateVelocity`, `UnitDirection2`, `PlateKinematics`, `PlateBoundaryEvaluator`, `PlateBoundaryEffect` ;
- snapshot : `PlateAtlasBuilder.Build(identity, atlas, frozenProfile, settings)`, `PlateAtlasSnapshot`, `PlateDomain`, `PlateCellState`, `PlateBoundaryRecord`, `CrustKind` ;
- continents : `ContinentalFieldModel.Create(identity, frozenProfile, settings)`, `ContinentalRaster`, `ContinentalSamplingGrid`, `ContinentalTopologyAnalyzer`, `ContinentalTopologyReport` ;
- budget : `PlateGenerationSettings.MaximumBoundaryInfluenceEvaluations`.

Les unités non calibrées sont explicitement `Normalized` ou `Ppm`. L03-B devra enrichir le snapshot avec résistance, solubilité, perméabilité et couches sans convertir implicitement ces valeurs en mètres. Aucun golden partagé n'a été changé ; les preuves locales sont reproductibles depuis les seeds, profils gelés, hashes d'identité et commit fonctionnel.

## Risques et blocages

- T03-02 reste `REVIEW_REQUIRED` jusqu'à revue qualitative indépendante des cartes hashées ; les propriétés numériques et l'inspection de cohérence de l'auteur ne valent pas cette revue.
- Les fonctions trigonométriques sont qualifiées sur Windows/.NET 10 seulement ; aucune reproductibilité binaire universelle n'est revendiquée.
- Le budget couplé est volontairement conservateur (`toutes arêtes × toutes cellules`) et peut refuser un atlas dont peu d'arêtes sont réellement inter-plaques ; le caller doit fournir un budget explicite cohérent avec son profil.
- Les contours sont exacts pour la classification raster, mais ne constituent pas encore le modèle vectoriel destiné à la voxelisation.
- Les seuils T03-02 sont une politique de test provisoire visible, puisque le document normatif n'en fixe aucun ; leur acceptation appartient à l'intégrateur.
- Mapping vers blocs, relief voxel, matériaux et stratification : hors périmètre L03-A, prévu dans L03-B/L03-C.
- Blocage d'implémentation ou de test Core : aucun. Revue indépendante : requise.

## Reprise exacte

Le commit fonctionnel `01dcbfb6cde5736c754d440ea63a99194d81e42a` porte les quatre correctifs de revue. Le commit suivant porte uniquement cette passation actualisée. Aucun processus de jeu, Visual Studio/MCP ou test n'est actif ; verrou MCP non acquis. Les sorties `bin/`, `obj/` et `.local/L03A/` sont ignorées par Git.

Prochaine action limitée : relire le HEAD exact transmis, vérifier les hashes ci-dessus, puis laisser l'intégrateur seul fusionner, relancer la régression sur `main` et changer l'état global. Le sous-agent ne pousse pas et ne modifie pas `registry/state.json`.
