# Passation — L05-C

**Statut proposé : REVIEW.** Correctif P1 appliqué après revue.

## Identité

| Champ | Valeur |
|---|---|
| Tâche / propriétaire | L05-C / agent:l05-c |
| Branche / worktree | `agent/l05-c` / `ISRWorldGen-worktrees/l05-c` |
| Commit de base | `bfb2746` |
| Contrats consultés | C02 v1.0, C03 v1.0 |
| Runtime | Vintage Story 1.21.6.1, seulement comme référence de compilation; aucune session de jeu lancée |

## Résultat

Implémentation limitée à `Hydrology/Boundaries` :

- publication de `BoundaryPortSnapshot` immuable et canoniquement triée par `StableId`;
- `PortId` dérivé exclusivement de `(OwnerId, OwnerLocalIndex)` dans le domaine Hydrology;
- plan partagé canonisé, franchissement quantifié, niveau, profil de lit, débit, corridor protégé, itération et signature de relief;
- raffinement immuable relié à un snapshot parent exact : provenance/plan/corridor obligatoires, IDs fins uniques, addition déterministe et conservation stricte `somme enfants + reliquat parent = budget parent`;
- aucune mutation ni recalcul du port parent publié.

Correctif P1 : chaque enfant public est maintenant revalidé (ID dérivé de `OwnerId`/`OwnerLocalIndex`, plan canonique, profil eau/lit, corridor propre et corridor parent) avant d'être normalisé puis trié par `(ParentPortId, ChildPortId)`. Aucune somme flottante ni décision d'allocation ne lit l'ordre de l'énumération appelante.

L'interface est délibérément bornée dans ce sous-lot car C03 décrit l'objet mais ne fournit pas encore de type `BasinPlan`/`RiverReach` partageable. L06/L07 peuvent consommer `BoundaryPortSnapshot` et `BoundaryRefinementSnapshot` sans état global, cache, chunk ou horloge.

Exigences couvertes : R05-05 et R05-06 (preuves Core analytiques).

## Tests et preuves

| Test | Commande réellement utilisée | Statut | Rapport / preuve |
|---|---|---|---|
| T05-05 | `dotnet test ... Debug --filter FullyQualifiedName~L05C` | PASS (6/6) | `artifacts/test-results/L05C/debug/L05C-debug-p1.trx` |
| T05-06 | même campagne Debug | PASS (6/6) | `artifacts/test-results/L05C/debug/L05C-debug-p1.trx` |
| Régression L05-A/B/C | `dotnet test ... Release --filter "FullyQualifiedName~L05A\|FullyQualifiedName~L05B\|FullyQualifiedName~L05C"` | PASS (18/18) | `artifacts/test-results/L05C/release/L05ABC-release-p1.trx` |

Les deux commandes ont employé `-p:VintageStoryPath="D:\Jeux\Vintagestory"`; la valeur d'environnement par défaut pointait vers `assets`, qui ne contient pas l'assembly API. Restauration verrouillée exécutée avant les tests. Les scénarios couvrent explicitement le fleuve à quatre régions dans les ordres inverses, trois raffinements/rechargements, permutations finies hétérogènes et exactitude binaire des allocations, ID dupliqué ou falsifié, corridor invalide, profil eau/lit invalide, dépassement budget, provenance/itération incompatibles, débit non fini et overflow.

## Contrats et impacts

Aucun contrat partagé, registre, projet, dépendance ou version modifié. Nouvelle API bornée sous `ISRWorldGen.Core.Hydrology.Boundaries`; consommateurs candidats L06/L07. Les snapshots portent `AlgorithmVersion = 1`, itération et signature de relief. Aucun golden à régénérer.

## Risques et blocages

Le raccordement est validé au niveau Core seulement : `BasinPlan`, `RiverReach` et la matérialisation voxel relèvent des lots ultérieurs. Les coordonnées et niveaux sont des quantifiés entiers sans unité de conversion publique tant que le contrat géométrique amont n'est pas publié. Aucun blocage pour la revue locale.

## Reprise exacte

Correctif P1 à committer : `src/WorldGen.Core/Hydrology/Boundaries/BoundaryRefinement.cs`, `testsrc/WorldGen.Tests/L05C/BoundaryPortsTests.cs`, ce journal. Aucun processus actif ni verrou MCP. Prochaine action : revue intégrateur, puis fusion et régression élargie.

Notification : `L05-C | Correctif P1 prêt | Hydrology/Boundaries | Réduction canonique et validation enfant complète, R05-05/R05-06 Core couvertes, statut REVIEW | TRX Debug 6/6 et Release L05-A/B/C 18/18 | Nouvelle revue et intégration attendues`.
