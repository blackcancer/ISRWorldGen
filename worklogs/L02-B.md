# Passation — L02-B
**Statut proposé : REVIEW.** Seul l’intégrateur peut déclarer `DONE`.

## Identité
- Tâche : L02-B — indexer et borner les descriptions globales.
- Propriétaire réel : `agent:l02_b`.
- Branche / worktree : `agent/l02-b` / `E:\Développement\Vintage Story\ISRWorldGen-worktrees\l02-b`.
- Commit de base : `aa21c63dfed3ca9edbf654c22cb3223279da118e`.
- Commit livré : HEAD contenant cette passation ; le hash exact est fourni dans le HANDOFF et la preuve structurée post-commit ignorée.
- Dépendance : L02-A `DONE` dans cette base, commit intégré `ed504555f948d3470727fab9f67b855e9b0c15cf`.
- Contrats consommés sans modification : S02 v1.0, C00 v1.0, C01 v1.0 ; schéma snapshot 1 et algorithme RNG 1.
- Qualification locale : SDK .NET `10.0.400`, runtime `.NET 10.0.11`, Windows X64.
- Visual Studio / MCP / Vintage Story / GPU / sauvegarde : non utilisés ; L00-C conserve le verrou exclusif du jeu.

## Résultat

### R02-03 — atlas borné, complet et compact
- `AtlasIndexProfile` fige `WorldDomain`, `ScaleModel`, taille de tuile, nombre de sites demandé, quota de sites et budget mémoire. Le nombre de workers et le mode de cache n’entrent ni dans l’identité ni dans le checksum géographique.
- `AtlasSpatialIndexPlanner` calcule avant toute génération les octets modélisés des sites, du graphe compact, de l’index spatial et de la réserve de travail du maillage exact. Un quota dépassé, une capacité CSR dépassée ou un budget insuffisant rend `BudgetExceeded` sans snapshot.
- Le chemin T02-03 réel n’est pas réduit aux sites : il exécute `AtlasSiteGenerator`, le `AtlasGeometryBuilder` exact de L02-A, la projection immuable du maillage en `CompactAtlasGraph` (sites, aires, masques, arêtes et voisins CSR), puis `CompactSpatialIndex`. Aucun voxel n’est généré.
- Le snapshot final porte un `SnapshotHeader` C00 avec identité, étape, révision, dimensions, unités et checksum canonique SHA-256. Le flux canonique encode explicitement entiers big-endian, IDs, rationnels, textes UTF-8, graphe CSR et index trié.
- Le checksum exclut budget, workers, cache, métriques GC et PID. Les ordres linéaire/inverse/permuté, 1/2/16 workers et caches froid/précalculé produisent le même contenu.
- Aucune grille par colonne globale n’est allouée. À nombre de sites identique et sans primitive, les plans de snapshots des mondes 1 024² et 1 024 000² sont identiques bien que le second représente 1 048 576 000 000 colonnes.

Profil qualifié T02-03 : **atlas sparse sous le budget candidat 256 Mio, seuil non gelé ; pas un preset final ni une validation de densité**.
- Monde : 1 024 000 × 1 024 000 blocs, hauteur 384, tuile 16 384 blocs.
- Seed : -437287116 ; requested sites : 56 ; quota : 8 192 ; budget candidat : 268 435 456 octets.
- Sortie : 56 sites, 155 arêtes, 310 références de voisinage ; snapshot estimé 13 948 octets.
- Plan : sites 1 816 octets ; graphe compact 11 260 ; index spatial 2 688 ; réserve de travail géométrique 222 298 112 ; plan de pic additionnel 222 391 932, soit sous le budget candidat de 268 435 456 octets.
- Deux processus après warm-up ont observé chacun 137 422 464 octets **cumulés alloués** par `GC.GetTotalAllocatedBytes(precise:true)`. Ce cumul n’est pas un pic vivant et n’est pas comparé au plan de pic.
- Le delta de mémoire managée vivante observé séparément par `GC.GetTotalMemory` après collection complète est 13 600 octets dans les deux processus. Ce delta signé n’est pas un working set système.
- Collections GC pendant la fenêtre mesurée : Gen0=10, Gen1=2, Gen2=2 dans les deux processus.
- Checksum : `32592a5e70ad5f948c1d6bea3d5110675390b08c4dfb1261901c13a507b0e5d6`.

Un profil laboratoire supplémentaire 64 sites / quota 8 192 / budget 512 Mio exécute le même chemin complet. Son plan est 285 309 852 octets et son checksum `8f1547bc66d97cba0db5aeb0db8d28fc44f552709673aa577e5856be2e69f982`. Il n’est pas utilisé pour revendiquer le respect du budget candidat 256 Mio.

Le premier essai tests-first avec 256 sites a révé environ 3 006 551 656 octets cumulés alloués avec l’ancienne mesure current-thread, invalidant une première réserve de 269 661 788 octets. Ce rouge est conservé comme défaut de planification, pas transformé en PASS. Avec la réserve corrigée, le cas 256 sites / quota 8 192 exige un plan de 4 311 922 332 octets et est refusé contre un budget 512 Mio avant appel L02-A ; le refus n’a cumulé que 2 200 octets, retourne `BudgetExceeded` à `atlas.spatial-index.budget` et ne publie aucun snapshot.

Aucune qualification de 262 144 sites, de densité finale ou de preset de production n’est revendiquée.

### R02-04 — ownership et index local
- Une primitive immuable porte ID, type, description NFC, polyligne globale et bounding box semi-ouverte. La polyligne est copiée dès l’entrée puis à la publication.
- L’index stocke seulement les tuiles occupées, des offsets CSR et des indices de primitives. Une requête locale renvoie des candidats uniques triés ; aucun objet n’est dupliqué comme décision.
- L’indexation est volontairement conservative par intersection de bounding box. Elle peut renvoyer un candidat que le consommateur précisera contre la polyligne ; elle ne crée ni second placement ni second ID.
- L’owner canonique est la tuile touchée de plus petit ordinal global `(Z,X)`. Son `OwnerId` dérive d’un namespace stable de tuiles et de cet ordinal, jamais de la première requête.
- La fixture F07 comprend une rivière et une caverne fictive traversant quatre tuiles 512 × 512. Chaque tuile retrouve exactement les deux mêmes ID/descriptions/OwnerId, soit 8 références vers seulement 2 primitives. La requête nord-est conserve l’owner sud-ouest `(0,0)` hors de la tuile consultée.
- Les primitives exactement sur `x=512` n’appartiennent qu’à la tuile est. Les clés négatives ou au-delà du monde rendent une collection vide ; aucun wrap torique n’est possible.
- Preuve inter-processus ownership : checksum `50ee61756f5ffbe1e1e063a1b792c77d2999fe6a877a627423a08e56b2703fac`, owner `7764cd403e4ebe2156638e50b85810a3`, deux objets dans la requête nord-est.

## Fichiers livrés
- Production : `src/WorldGen.Core/Atlas/SpatialIndex/SpatialPrimitives.cs`, `AtlasSpatialIndexPlanner.cs`, `CompactAtlasSnapshot.cs`, `AtlasSpatialIndexBuilder.cs`.
- Tests : `testsrc/WorldGen.Tests/L02B/SpatialIndexTestSupport.cs`, `VastAtlasBudgetTests.cs`, `CanonicalOwnershipTests.cs`, `SpatialIndexProcessProbeTests.cs`.
- Runner de preuve : `testsrc/WorldGen.Tests/L02B/Invoke-L02BProcessDeterminism.ps1`.
- Passation : `worklogs/L02-B.md`.

## Tests et preuves
| Test | Commande | Statut proposé | Rapport / preuve |
|---|---|---|---|
| Cycle tests-first initial | filtre `FullyQualifiedName~ISRWorldGen.Tests.L02B` avant production | RED attendu | compilation rouge : namespace et 17 symboles SpatialIndex absents ; aucun faux PASS |
| Première mesure 256 sites | même filtre après première implémentation | RED utile | 6/8 ; cumul ~3,01 Go supérieur au plan initial ~270 Mo, plan corrigé et profil de qualification borné |
| T02-03 + T02-04 ciblés | `dotnet test testsrc/WorldGen.Tests/WorldGen.Tests.csproj -c Release --no-restore --filter FullyQualifiedName~ISRWorldGen.Tests.L02B` | PASS | 8/8, zéro échec/ignoré ; TRX final post-commit sous `artifacts/test-results/L02B/` |
| T02-03 vaste atlas complet | chemin site→maillage exact→graphe compact→index | PASS qualifié sparse sous budget candidat non gelé | profil 56/256 Mio et mesures ci-dessus ; profil laboratoire 64/512 Mio séparé ; aucune allocation proportionnelle aux colonnes ; refus pré-allocation 256 et quota 8 193 testés |
| T02-04 propriété canonique | rivière+caverne F07, quatre tuiles, ordres/workers/caches | PASS | 2 primitives, 4 tuiles, 8 références, owner unique hors tuile interrogée, aucune duplication/wrap |
| Redémarrage réel | `Invoke-L02BProcessDeterminism.ps1` | PASS | deux `dotnet test` distincts, PIDs distincts ; même commit/framework/architecture/versions/config, plans, checksums atlas parallèle et ownership, refus dense |
| Régression L01-A/B/C + L02-A/B | `dotnet test ... -c Release --no-build --no-restore` après build Tools | PASS | 84/84, zéro échec/ignoré |
| Builds hors jeu | Core et Tests, Debug puis Release, `--no-restore` | PASS | quatre builds, chacun 0 avertissement / 0 erreur ; Tools Release construit uniquement comme prérequis L01-C, 0/0 |
| Solution/adaptateur Vintage Story | non exécuté conformément au verrou | NOT_RUN | verrou exclusif L00-C et hors périmètre L02-B ; aucun chemin jeu substitué |
| Format/analyse | `dotnet format` Core puis Tests `--no-restore --verify-no-changes` | PASS | aucun écart ; parse PowerShell PASS, PSScriptAnalyzer non installé |
| Hygiène | `git diff --check`, inspection portée et secrets | PASS attendu au commit | uniquement chemins L02-B ; aucun secret attendu |

La restauration initiale du worktree a utilisé `dotnet restore ISRWorldGen.sln --locked-mode` avec une configuration NuGet temporaire officielle provenant du worktree L02-A. Toutes les recettes qualifiantes ultérieures sont `--no-restore`.

## Contrats et impacts
Aucun `.csproj`, solution, lock NuGet, contrat/specification partagé, registre, Foundation, Geometry L02-A, outil ou autre lot n’a été modifié. Aucune dépendance externe n’est ajoutée.

L’API publique ajoutée est confinée à `Atlas/SpatialIndex`. Le hash `ISRW-ATLAS-INDEX-V1` est un format canonique interne du nouveau snapshot ; toute modification de son encodage exige de versionner sa marque et d’ajouter un test de compatibilité.

## Risques et limites
- La réserve géométrique O(n²) est une estimation de planification conservatrice qualifiée sur le profil candidat 56 sites et son corpus, pas une preuve de pic dur pour tous les nombres de sites ou toutes les plateformes. Le seuil 256 Mio reste candidat et non gelé. Le cumul alloué, le delta managé vivant et les collections GC restent rapportés séparément.
- Le maillage exact L02-A devient rapidement coûteux en allocations ; le rouge 256 sites interdit de promouvoir le profil sparse en preset final. Un futur changement d’algorithme ou une campagne de densité demande un lot/contrat dédié.
- L’index de bounding boxes est un index de candidats, pas un test d’intersection polyligne-tuile exact.
- La qualification couvre Windows X64/.NET 10.0.11. Aucune promesse cross-platform, moteur, densité finale ou profil 262 144 sites n’est formulée.
- PSScriptAnalyzer n’est pas installé ; le script est parsé par PowerShell et exécuté deux fois avec succès.
- La solution et l’adaptateur Vintage Story sont `NOT_RUN (verrou exclusif L00-C, hors périmètre L02-B)`.

## Reprise exacte
Sur le HEAD livré, relancer les 8 tests ciblés, les 84 tests de régression après build Release de Tools, puis `Invoke-L02BProcessDeterminism.ps1`. Vérifier que la preuve post-commit cite exactement le HEAD et conserve les checksums qualifiés. Aucun processus, session Visual Studio/MCP/jeu ou verrou L00-C n’est utilisé par ce lot.
