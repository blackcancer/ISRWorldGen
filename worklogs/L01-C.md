# Passation — L01-C
**Statut proposé : REVIEW.** Seul l’intégrateur peut déclarer `DONE`.

## Identité
- Tâche : L01-C — établir le banc analytique et le harnais de tests.
- Propriétaire réel : `agent:l01_c`.
- Branche / worktree : `agent/l01-c` / `E:\Développement\Vintage Story\ISRWorldGen-worktrees\l01-c`.
- Commit de base : `9683c595b526a81e01307d3fd9ef4c162b4d5014`.
- Commit livré : HEAD contenant cette passation ; le hash exact est fourni dans le HANDOFF et dans la preuve structurée post-commit ignorée.
- Dépendance : L01-B vérifiée `DONE` avant démarrage.
- Contrats consommés sans modification : S01 v1.0, C00 v1.0, schéma snapshot 1, algorithme RNG 1.
- Qualification locale : SDK .NET `10.0.400`, runtime `.NET 10.0.11`, Windows X64.
- Visual Studio / MCP / Vintage Story / GPU / sauvegarde : non utilisés ; aucun verrou jeu détenu.

## Résultat

### R01-05 — banc Core hors jeu
- Le runner `ISRWorldGen.Tools` expose `run` et `audit-dependencies` avec schéma de rapport v1 et grammaire fermée : option manquante, inconnue, dupliquée ou invalide produit `USAGE_ERROR`/code 2.
- Codes de sortie figés : succès 0, exécution échouée 1, usage invalide 2, fixture/test absent 3.
- Deux fixtures analytiques réelles sont intégrées : plan incliné `plane-x` et selle `saddle`, chacune 9 × 9 échantillons. Les paramètres seed int32, hash de configuration, commit, ordre, workers, cache, faute et budget sont obligatoires et réexportés.
- Ordres disponibles : linéaire, inverse et permutation déterministe. Workers : 1 à 256. Cache : froid ou prérempli chaud. Ces paramètres d’exécution restent hors de l’identité géographique et des hashes.
- L’export JSON numérique minimal publie compte, minimum/maximum en 1/256 bloc, SHA-256 du snapshot hauteur canonique et SHA-256 du snapshot voxel Core canonique.
- L’auditeur lit les tables PE/metadata des binaires compilés, suit la fermeture des assemblages projet locaux, inspecte les références directes et transitives, les méthodes P/Invoke et le `.deps.json`. Il ne se contente pas de rechercher des namespaces source.
- La preuve hors jeu force `VINTAGE_STORY` absent et `VintageStoryPath=Z:\ISRWorldGen-NoGame`, puis compile Core/Tools/Tests, exécute les tests découverts, audite les dépendances et lance une fixture. Le script peut être appelé depuis un autre répertoire.
- Le script parse les compteurs TRX : un filtre découvrant zéro test rend `TEST_ABSENT`/code 3 même si `dotnet test` lui-même rend 0.

### R01-06 — échec atomique
- Frontières injectables : `validate-input`, `prepare-fixture`, `generate-data`, `validate-snapshot`, `publish-snapshot`.
- Chaque annulation rend un `GenerationFailure<AnalyticalSnapshotBundle>` contenant `GenerationError`/`Cancelled`, sans propriété snapshot. Le candidat construit avant une annulation de publication reste interne et n’est jamais publié.
- Budget insuffisant rend `BudgetExceeded` à `generate-data`; l’injection `nonfinite` introduit réellement `double.NaN` et rend `InvalidInput` à `validate-snapshot`.
- Tout rapport d’échec contient la cause typée, l’étape, le hash d’entrée et `publication.visible=false` avec métriques nulles.
- Après chacune des cinq annulations, après la valeur non finie et après le budget dépassé, un nouveau processus relance la seed 42 sans faute et compare exactement ses deux hashes au témoin.

Témoin seed 42, config `9e12605ff5e0e94ccccf28eac0ded526da1e687a1fd3a0650b782da7484906ed`, fixture `plane-x` :
- données : `26d848e460fabe38cac29e575ccdd0e16af00cb23963772c410229501da3481e` ;
- voxels Core synthétiques : `a105f9b74a038c85a49db1327ff88f2e0ad24b8bf8cc0adf4c4c0570f6557a60` ;
- 81 échantillons et 81 voxels ; minimum 15 905/256 blocs, maximum 20 980/256 blocs.

Ces hashes sont des témoins de preuve, pas des goldens auto-régénérés. Aucun fichier golden n’est nécessaire pour T01-05/T01-06.

## Fichiers livrés
- Production : `src/WorldGen.Tools/TestHarness/Program.cs`, `HarnessApplication.cs`, `AnalyticalPipeline.cs`, `CompiledDependencyAuditor.cs`.
- Tests : `testsrc/WorldGen.Tests/L01C/HarnessCliTestSupport.cs`, `HarnessRunnerTests.cs`, `FaultInjectionTests.cs`, `OfflineDependencyTests.cs`.
- Runner de preuve : `testsrc/WorldGen.Tests/L01C/Run-L01C-Offline.ps1`.
- Passation : `worklogs/L01-C.md`.

## Tests et preuves
| Test | Commande | Statut proposé | Rapport / preuve |
|---|---|---|---|
| Cycle tests-first | filtre `FullyQualifiedName~L01C` avant implémentation | RED attendu | `artifacts/test-results/L01C/red/L01C-red.trx` : 0/8, 8 échecs fonctionnels contre le placeholder |
| T01-05 + T01-06 ciblés | `dotnet test testsrc/WorldGen.Tests/WorldGen.Tests.csproj -c Release --no-restore --filter FullyQualifiedName~L01C` | PASS | 8/8, zéro échec/ignoré |
| Régression L01-A/B/C | `dotnet test testsrc/WorldGen.Tests/WorldGen.Tests.csproj -c Release --no-build --no-restore --logger "trx;LogFileName=L01-ABC-review-precommit.trx" --results-directory artifacts/test-results/L01C/review-precommit` | PASS | 64/64, zéro échec/ignoré |
| T01-05 hors jeu | `Run-L01C-Offline.ps1` lancé depuis `E:\Développement\Vintage Story`, chemin jeu forcé invalide | PASS | `offline-proof.json` : 64/64, trois builds, audit et smoke verts |
| Zéro test découvert | même script avec filtre `FullyQualifiedName~ISRWorldGen.Tests.DoesNotExist` | PASS du contrôle négatif | `offline-proof.json` : `TEST_ABSENT`, total 0, code exact 3 vérifié par l’appelant |
| Builds hors jeu | Core, Tools et Tests, Debug puis Release, `--no-restore` | PASS | six builds, chacun 0 avertissement / 0 erreur |
| Audit compilé | commande `audit-dependencies` intégrée à la preuve | PASS | racines Core+Tools ; 9 références directes, 6 transitives, 0 interdite, 0 P/Invoke, 0 package |
| Format/analyse | `dotnet format ISRWorldGen.sln --no-restore --verify-no-changes --verbosity diagnostic` | PASS | 48 fichiers analysés, 0 fichier à reformater |
| Hygiène | `git diff --check`, inspection portée et secrets | PASS | uniquement chemins L01-C ; aucun secret détecté |

La restauration initiale du worktree a utilisé `dotnet restore ISRWorldGen.sln --locked-mode` avec une configuration NuGet temporaire ignorée. Toutes les recettes qualifiantes sont ensuite `--no-restore`.

## Tests-first et corrections
Les huit tests L01-C ont été compilés et exécutés contre le placeholder Tools : 0 réussite et 8 échecs attendus. Après implémentation, le premier passage fonctionnel avait 6/8 réussites ; les deux autres échecs ont révélé l’ordre des arguments propre aux assertions de comparaison MSTest 4, puis le passage est devenu 8/8. Le runner négatif zéro-découverte a confirmé que `dotnet test` rendait 0 malgré zéro cas ; le parseur TRX et le code 3 empêchent cette fausse qualification.

## Contrats et impacts
Aucun `.csproj`, solution, lock NuGet, contrat partagé, Foundation, registre ou autre lot n’a été modifié. Le runner consomme seulement `WorldGen.Core` et des bibliothèques de base .NET. L’identité de génération existante et le sens des snapshots hauteur/voxel restent inchangés.

Tout ajout de fixture, de frontière, de code de sortie ou changement de schéma JSON du runner devra être versionné et accompagné d’un test de compatibilité. Tout changement de C00 passe par l’intégrateur.

## Risques et blocages
- Les voxels sont le payload synthétique Core de C00, pas une écriture Vintage Story, un test moteur ou une validation visuelle.
- La preuve d’indépendance couvre Windows X64/.NET 10.0.11 et la fermeture compilée Core/Tools ; aucune promesse cross-platform n’est formulée.
- Le build de la solution complète avec l’adaptateur Vintage Story est `NOT_RUN` dans la configuration volontairement sans jeu : T01-05 qualifie explicitement les trois projets hors jeu. Aucun contournement de l’adaptateur ni installation commerciale n’a été utilisé.

## Reprise exacte
Sur le HEAD livré, relancer le script hors jeu depuis un répertoire extérieur, puis le contrôle zéro-découverte et les six builds Debug/Release. Vérifier que la preuve post-commit cite exactement le HEAD, 64 tests découverts et les mêmes hashes géographiques. Aucun processus, session Visual Studio/MCP/jeu ou verrou ne doit rester actif.

## Validation de l'intégrateur sur `main`

Le commit accepté a été intégré sous `87b0d3878c99c14863faa36cd806726b994927ba`. La preuve hors jeu a été relancée depuis `E:\Développement\Vintage Story` avec `VINTAGE_STORY` absent et `VintageStoryPath=Z:\ISRWorldGen-NoGame`. Une première tentative a correctement produit `FAIL/commit unknown` parce que le compte de bac à sable refusait la propriété Git du checkout ; elle reste conservée sous `.local/L01C-integrated/offline`. Le rejeu final a utilisé une exception `safe.directory` limitée au seul processus, sans modifier la configuration Git, et a produit `.local/L01C-integrated/offline-final/offline-proof.json` avec le commit exact, statut PASS et 64/64 tests.

L'audit compilé intégré retrouve 9 références directes, 6 transitives, 0 interdite, 0 P/Invoke et 0 dépendance de package. Le smoke seed 73, ordre permuté, 2 workers, cache chaud reproduit les hashes `b90a73345fcd9fbe2104c489c3dd66832443c3dbaf1251f0d5821762e830f66a` et `6df0239692a04b9ba34fef20c5dc9eef535aa2cb747fbbd42ccda0bed0f66778`. Le témoin distinct seed 42, ordre linéaire, 1 worker, cache froid conserve les hashes documentés dans la passation ; il ne s'agit pas de la même exécution.

Le filtre volontairement inexistant a produit `.local/L01C-integrated/zero-final/offline-proof.json`, statut `TEST_ABSENT`, 0 test découvert et code exact 3. Les six builds Core/Tools/Tests Debug et Release avec chemin jeu invalide ont tous terminé à 0 avertissement/0 erreur ; le format et le diff-check sont verts. Verdict intégrateur : `DONE` pour R01-05/R01-06 et T01-05/T01-06 sur Windows X64/.NET 10.0.11, sans qualification du moteur Vintage Story.
